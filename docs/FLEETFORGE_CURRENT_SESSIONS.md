# FleetForge — Current Sessions Queue

**Purpose:** Forward-looking queue of sessions discussed in planning but not yet shipped. Companion to FLEETFORGE_PROGRESS.md (which is the historical archive).

**Discipline:**
- Every session discussed in planning (claude.ai web chat) gets an entry here at the moment it's discussed, not retroactively.
- Each entry includes: session label, scope summary, estimated effort, dependencies, status, source-of-discussion timestamp.
- When a session ships, its entry is marked SHIPPED with the commit refs and date, then archived to PROGRESS.md SESSION LOG. The entry can be removed from this file or kept with SHIPPED tag for a short grace period — operator's call.
- When a session is descoped or superseded, mark DEFERRED or SUPERSEDED with rationale; do not delete silently.
- Update this file in lockstep with FLEETFORGE_PROGRESS.md whenever ship-state changes.

---

## Pre-flight check (D136)

Per D136, every Code Desktop session begins with a pre-flight read of this file:

1. Read FLEETFORGE_CURRENT_SESSIONS.md.
2. If any session is marked IN-FLIGHT with Touching domains overlapping the planned scope → halt and surface to operator for direction (serialize and wait, or opt into branch isolation per D136 fallback). IN-FLIGHT-RO sessions never block write sessions; multiple IN-FLIGHT-RO may coexist.
3. If no IN-FLIGHT collision, register the new session entry here with status IN-FLIGHT (write) or IN-FLIGHT-RO (read-only), start timestamp, agent identifier, and Touching domains (write only). Commit the registration as part of the session's first commit, OR as a standalone single-line edit if the operator wants to register before any work.
4. On session end, update the entry to SHIPPED (or DEFERRED / SUPERSEDED as applicable) per existing CURRENT_SESSIONS.md discipline. Remove the IN-FLIGHT multi-line details.

Read-only sessions skip the collision check (step 2) but still register (step 3) for operator visibility.

**Category separation (K-14, locked 2026-05-12 via S-PREDEPLOY-CHECKLIST-CREATE):** This file is the **session queue**. Items that surface during a session and are bug-shaped (a defect in shipped code) file to `FLEETFORGE_PROGRESS.md` → KNOWN ISSUES (`#NNN`). Items that are pre-deploy-shaped (a `.env` key needing a real value, an AWS resource to provision, a DNS record, a smoke/rollback procedure) file to `FLEETFORGE_PREDEPLOY_CHECKLIST.md` in the appropriate category — NOT here, and NOT to KNOWN ISSUES. Filing under the wrong category is documentation divergence (K-12 class).

---

## Active planning chat handoff

**AWS Lightsail cutover chat** — PRODUCTION PARTIALLY LIVE 2026-05-16.
Server: fleetforge-prod (Oregon us-west-2, 4GB/2vCPU, Ubuntu 22.04). Static IP: 44.226.100.133. Domain: mainlandrentals.com (GoDaddy DNS). SSL: Let's Encrypt via certbot. App: https://mainlandrentals.com/fleetforge. DB: MySQL 8.0, 19 migrations applied (17 ok / 0 drift / 0 missing at apply time; 2 manually marked due to FK constraints on fresh DB — the rate-cards seed migration `202605130934_S-SEED-RATE-CARDS-LOAD_standard_2025_default.sql` references a `created_by` user ID that doesn't exist yet on a fresh DB; remediated in queue via S-RATE-CARDS-PROD-FIX). Super admin account: created (via mysql + bcrypt) and verified login working. Outstanding operator server tasks: see `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md` items B6 (SES SMTP credentials, blocked on sandbox approval) + D10 (cron jobs) + D11 (S3 storage test) + D12 (CloudWatch billing alarm) + D13 (SNS topic + SES bounce webhook) + E1 (rate cards seed, blocked on S-RATE-CARDS-PROD-FIX) + G5 (second admin user) + G6 (super admin MFA enrollment) + I4 (error log monitoring) — 9 items total. Outstanding Code Desktop tasks: `S-COMPOSER-LOCK-FIX` + `S-RATE-CARDS-PROD-FIX` (both QUEUED below).

Original handoff context (preserved): all Tier 1/2/3 sessions complete (23 SHIPPED labels in this file as of HANDOFF-PREP). Model B mileage arc closed (engine via S-MILEAGE-1/-2A/-2B/-3/-3-FIX-0/-5; portal + helper retirement via S-PORTAL-MILEAGE-MODEL-B). Doc freshness smoke green (17/17 PASS exit 0). 17 migrations applied at handoff-prep time (now 19 post-S-AUTH-FIX + S-SETTINGS-CLEANUP), 19/0/0 sticky on local + prod. Primary working doc: `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md`. This chat (original) retains history. K-16: trust canonical files; do not re-derive locked decisions. The QuickBooks integration chat (`FLEETFORGE_QUICKBOOKS_SPEC.md`) is a separate planning chat, not yet started.

Remaining open queue items independent of cutover and QBO arcs:
- `S-MILEAGE-3-ACCT-SPEC` — CPA-blocked on 5 enumerated questions (D-I (A) / D176).

---

## Status legend
- **QUEUED** — discussed, prompt drafted (or near-drafted), not yet shipped
- **IN-FLIGHT** — write-mode session currently executing in Code Desktop. Per D136, only one allowed at a time on main. Entry includes start timestamp, agent identifier, and touching domains.
- **IN-FLIGHT-RO** — read-only session (audit, survey, decision-surfacing) running concurrently. Multiple IN-FLIGHT-RO may coexist with one IN-FLIGHT.
- **BLOCKED** — waiting on operator input or upstream session
- **SHIPPED** — landed on origin/main; archive to PROGRESS.md
- **DEFERRED** — scoped out indefinitely with reason
- **SUPERSEDED** — replaced by another session with reason

---

## Active session format

For sessions in IN-FLIGHT or IN-FLIGHT-RO status, render the entry across multiple lines:

**S-EXAMPLE-LABEL** — IN-FLIGHT
  Started: 2026-05-07T14:32 UTC by desktop-1
  Touching: lib/Billing/, app/admin/leases/, FLEETFORGE_DATABASE_MASTER.sql

Required fields: STATUS keyword, Started (ISO 8601 timestamp + operator-supplied agent identifier).
Optional metadata: Touching (file paths or directories the session expects to write — used for collision detection in pre-flight check).

For IN-FLIGHT-RO, the Touching field is not required (read-only doesn't lock):

**S-EXAMPLE-AUDIT** — IN-FLIGHT-RO
  Started: 2026-05-07T14:32 UTC by desktop-2

When the session ships, update the entry to status SHIPPED with commit refs (per existing convention) and remove the multi-line IN-FLIGHT details.

---

## Active queue (as of 2026-05-17)

### IN-FLIGHT

*(none)*

**S036** — SHIPPED 2026-05-19 (see PROGRESS.md SESSION LOG row)
Outcome: Phase B — Financial Reports + Budget module + AI integration. **Part A:** ReportingService.php (P&L, BS, CF, AssetSchedule). **Part B:** BudgetService.php (CRUD + copy + variance). **Migration:** 202605190030 extends ai_summaries.summary_type ENUM with 4 new values (pl_narrative, bs_narrative, cashflow_narrative, budget_variance) appended at end per D126; 26→27 migrations. **Part C:** 16 files — 4 report APIs (each with ?format=pdf via new ReportPdfRenderer using mPDF), 7 budget APIs, 3 saved-reports APIs, 3 settings INSERTed idempotently. **Part D:** 8 admin pages (4 reports + 4 budgets) + nav updates (Reports group + Budgets item). **Part E:** AI integration — 4 new tools in FleetForgeTools + ToolRegistry, SummaryEngine extended with 4 new gather methods + 4 CPA-appropriate prompts, api/v1/ai/summary.php extended (entity_type=accounting + 4 new summary_types + context body field). 3 prompt-vs-disk divergences resolved under K-22 (acc_budgets schema uses year/version/status not fiscal_year/basis/approved; saved_reports has no is_shared; acc_budget_lines.dec is a reserved word — backtick-quoted everywhere). Live smoke confirms all 4 report engines produce correct numbers. BS imbalance + CF tie-out fail as expected per STOP CONDITIONS (S-QBO-27 follow-up). D131 6/6 PASS pre-commit (PARITY + INVARIANTS I1-I10 + samsara 16/16 + model_b 20/20 + doc_freshness 17/17 + migrate 27/0/0). 3 decisions locked D-S036-1/2/3.

**S-ACCT-FIX-DOCS** — SHIPPED 2026-05-18 (commit 1b5d965 — see PROGRESS.md SESSION LOG row)
Outcome: 3 new accounting documents API endpoints (`upload.php` + `index.php` + `delete.php` under `api/v1/accounting/documents/`) + reusable `includes/partials/acc-documents-section.php` Alpine partial wired into 6 entity admin show pages (4 newly created: bills/show.php + journal-entries/show.php + fixed-assets/show.php + bank-transactions/show.php; 2 updated: ap-payments/show.php stub replaced, tax/show.php section added). 1-line View link from each transaction row in bank-accounts/index.php to the new bank-transactions/show.php. End-to-end smoke test PASS (Trap 7 enforced both ways, MIME rejection works, signed URLs serve, CSRF threaded correctly). One x-data attribute-encoding bug surfaced + fixed mid-flight (htmlspecialchars instead of JSON_HEX_QUOT). D131 gate clean pre-commit (26/0/0 sticky, no schema motion).

**S-ACCT-FIX-AP** — SHIPPED 2026-05-18 (commit 72ca97a — see PROGRESS.md SESSION LOG row)
Outcome: 6 orphan ap_payment JEs (JE-2026-00036/00038/00040/00042/00045/00047) reversed via JournalEntryService::reverse() — all DEMO-RESIDUE, source_id 5 absent because BILL-2026-00005 status='approved' not 'paid'. Reversal JEs JE-2026-00060–00065 carry idempotency tag `[FIX-AP-source_id-N]`. New AP-Payments admin pages (index + show) + API show endpoint + accounting-nav "Payments" entry under Payables, browser-verified in Claude Preview. Phase 4 root-cause fix: demo_accounting.php paid-bill block now writes acc_ap_payments + acc_ap_payment_allocations + UPDATE source_id wire-back; live create.php gets a post-insert subledger-row-exists assertion (defense-in-depth). 4 DECISIONS locked D-ACCT-AP-1 through -4. D131 gate clean pre-write + post-Phase-2 + pre-commit (26/0/0 sticky, no schema motion).

### Bug investigation outcomes

**S-COMPOSER-LOCK-FIX** — QUEUED
Scope: The 2026-05-16 production deployment ran `composer update` on the server to resolve PHP 8.2 compatibility (composer.lock had symfony v8.x packages requiring PHP >= 8.4). The updated composer.lock on the server differs from the one in the repo. This session pulls the updated composer.lock from the server and commits it to origin/main so future deployments don't require a manual composer update.
Steps: (1) copy updated composer.lock from server via scp or paste; (2) run `composer install --no-dev` locally to verify it resolves cleanly on PHP 8.2; (3) commit + push.
Effort: ~20 min. 2-commit arc.
Dependencies: none — can ship immediately.
Discovered: 2026-05-16 during Lightsail deployment.

**S-RATE-CARDS-PROD-FIX** — QUEUED
Scope: The Standard 2025 rate card migration (`202605130934_S-SEED-RATE-CARDS-LOAD_standard_2025_default.sql`) fails on a fresh database because it references a `created_by` user ID that doesn't exist yet (FK constraint on `rate_cards.created_by` → `users.id`). Fix: update the migration to use NULL for `created_by` (the column allows NULL per schema — `ON DELETE SET NULL`). Also re-run the rate card seed on production so Standard 2025 rates are available.
Steps: (1) update the migration SQL to use NULL `created_by`; (2) update `schema_migrations` checksum; (3) run the seed on prod manually via mysql; (4) commit fix.
Effort: ~30 min. 3-commit arc.
Dependencies: none — can ship immediately.
Discovered: 2026-05-16 during Lightsail deployment.

**S-AUTH-FIX** — SHIPPED 2026-05-14 (commits cea88fb-e31b25e + this C5 — see PROGRESS.md SESSION LOG)
Outcome: fixed session early-return bug (auth.php inactivity timeout fell through before remember-me check) + added MFA persistence via `users.mfa_verified_until`. "Keep me logged in for 30 days" now means no password AND no MFA re-prompt for 30 days. Security gap closed: remember-me with expired MFA restores session at password level but re-gates on MFA (no full logout). Migration 18.

**S-NOTIFICATION-URL-FIX** — SHIPPED 2026-05-14 (commits fb1685c-0964754 + this C3 — see PROGRESS.md SESSION LOG)
Outcome: 4 broken notification URL patterns fixed across 13 caller files. All notification types now route to existing pages. DB backfill skipped per operator decision (Option B); historical broken URLs age out as new correct notifications replace them. D131 gate green throughout.

**S-SETTINGS-CLEANUP** — SHIPPED 2026-05-14 (commits b4ca4c3-5a525d2 + this C4 — see PROGRESS.md SESSION LOG)
Outcome: MFA config (required_roles / totp_window / backup_code_count) now editable in Settings → Integrations → Security card (migration 19 backfilled labels; multi-checkbox UI for required_roles per D-A). Settings → Users tab replaced with link card to sidebar Users module (D-B); Portal Users tab unchanged. 3 DECISIONS locked D194-D196. D131 gate clean (C2 migrate 18→19 EXPECTED; PARITY OK retained for data-only UPDATE).

**S-NOTIFICATIONS-FULL** — SHIPPED 2026-05-14 (commits 2798e18 C1 + 69e233b C2 DB backfill + cef97e0 C3 messenger callers + this C4 docs — see PROGRESS.md SESSION LOG)
Outcome: invoice 404 on id=244 traced to soft-deleted invoice (D5 working as designed; no routing bug, no code change). 173 broken-URL rows in notifications table backfilled in a single db_transaction (166 known + 7 newly-surfaced rows missing /fleetforge prefix) via idempotent `scripts/fix_notification_urls_2026_05_14.php` per S-FIX-2 precedent. 3 messenger caller files patched to emit /fleetforge-prefixed URLs (same gap class S-NOTIFICATION-URL-FIX closed for other modules). Full destination-pattern audit: all 23 distinct URL forms map to existing pages with matching $_GET param names; notification UI renders url directly. NO missing pages, NO routing mismatches, NO migrations. D131 gate green throughout.

**S-DISPLAY-REVAMP** — SHIPPED 2026-05-14 (commits b62c5fe C1 + bd1ec62 C2 CSS + this C3 docs — see PROGRESS.md SESSION LOG)
Outcome: scope NARROWED at pre-flight per K-22. Part (A) "display settings non-functional" premise contradicted by file state — the system is fully wired end-to-end (DB cols + session + FOUC-free server bootstrap + density CSS + Alpine + API; real user has font=85 proving persistence). Part (B) sidebar badge fix shipped: 5-line CSS block under collapsed-sidebar media query zeroes min-width / padding / margin + adds overflow:hidden on `.sidebar:not(.is-open) .nav-badge`. Root cause: min-width:20px at line 788 beat max-width:0 at line 919, leaving a 20px-wide invisible badge in flex flow that pushed icons sideways. Browser-verified in running preview. FF_ASSET_VERSION 1.0.29 → 1.0.30; A5 added. 3-commit arc instead of 5.

### Documentation cleanup (queued, small)

**S-NGINX-PROD-CONFIG** — SHIPPED 2026-05-16 (commits 14b8e7c C1 + 07bd28b C2 runbook + G7 + this C3 docs — see PROGRESS.md SESSION LOG)
Outcome: locks D202 (production web server is nginx — not Apache; `.htaccess` inert; SCRIPT_FILENAME hardcoded to `/var/www/fleetforge/public/index.php`). Creates canonical `docs/runbooks/nginx_config.md` with CONTEXT + ROOT CAUSE OF ORIGINAL FAILURE (2026-05-16) + working sites-enabled config + HOW TO UPDATE + POST-CHANGE VERIFICATION. Adds PREDEPLOY G7 (✅ COMPLETE — config deployed + verified 2026-05-16: health.php 200/db:true, auth/login 200, dashboard 302). Root cause of all production AJAX failures at initial deploy now a permanent locked decision. No code/schema/migration touched.

**S-PROD-DEPLOYMENT-DOCS** — SHIPPED 2026-05-16 (commits 23c0d36 C1 + e3a88f3 C2 + this C3 docs — see PROGRESS.md SESSION LOG)
Outcome: documented all outstanding production tasks from the 2026-05-16 Lightsail deployment. Handoff status updated — production partially live at https://mainlandrentals.com/fleetforge (Oregon us-west-2, 44.226.100.133, 19 migrations applied, super admin verified). 2 Code Desktop sessions queued (S-COMPOSER-LOCK-FIX + S-RATE-CARDS-PROD-FIX). 8 new PREDEPLOY_CHECKLIST.md items filed (B6 SES SMTP creds, D10-D13 cron/S3/billing-alarm/SNS, E1 rate cards seed, G5-G6 second user + super admin MFA, I4 error log monitoring). Pre-flight verified all item numbers against PREDEPLOY high-water marks. Pure docs session — no code/schema/migration motion. D131 gate green throughout.

**S-CHECKLIST-WORDING-FIX** — SHIPPED 2026-05-14 (commits 11d5149 C1 IN-FLIGHT + d7f1d43 C2 fixes + this C3 docs — see PROGRESS.md SESSION LOG)
Outcome: 4 stale-wording fixes in `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md` surfaced by S-PREDEPLOY-FULL-VERIFY 2026-05-13. (1) I1 Status annotation added ("ready — S-PROD-2 SHIPPED 2026-05-02; ..."); (2) I1 Owner "(depends on B3)" qualifier removed; (3) G2 heading parenthetical `(I1-I6)` → `(I1-I10)`; (4) Last-touched stamp refreshed to 2026-05-14. No Action/Detail content changes; checklist is now internally consistent ahead of operator handoff to the Lightsail cutover sequence. D131 gate green throughout.

**S-K22-LOCK** — SHIPPED 2026-05-13 (commits 6727297 + 757d517 + this C3 commit — see PROGRESS.md SESSION LOG)
Outcome: locks K-22 (planning-chat prompt-drafting must trust on-disk canonical-file state over handoff-prompt narrative) in PROGRESS.md KEY LEARNINGS after K-21 + REFERENCE.md §0 LOCKED DECISIONS index pointer after K-14 row. Sourced from the 2026-05-13 3-session cleanup arc (S-CHECKLIST-DRIFT-FIX + S-DOCS-REORG-RESIDUAL + S-PROD-2-DOCS-RECONCILE). Companion memory entry at /Users/avi/.claude/projects/-Users-avi-Documents-fleetforge/memory/feedback_trust_file_over_prompt.md captures the Code-Desktop-side version; K-22 captures the planning-chat-side version. No code/schema motion; D131 gate green throughout.

**S-PROD-2-DOCS-RECONCILE** — SHIPPED 2026-05-13 (commits aff7879 + 56136a9 + this C3 commit — see PROGRESS.md SESSION LOG)
Outcome: docs-only reconcile after pre-flight surfaced that S-PROD-2 had actually SHIPPED 2026-05-02 (PROGRESS.md SESSION LOG line 104 + DECISIONS D75-D78 + production code) but CURRENT_SESSIONS.md carried a phantom QUEUED entry (introduced this morning by S-CHECKLIST-DRIFT-FIX C2 based on stale info) and PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 still cited "blocked on S-PROD-2". Originally arrived as 8-commit build prompt; operator authorized 3-commit docs-only reconcile via AskUserQuestion. CURRENT_SESSIONS.md S-PROD-2 entry flipped to retroactive SHIPPED 2026-05-02 reference + Recent ship history backfill added. PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 flipped "blocked" → "ready" with file-path accuracy corrections (B4 env-key name, D7 webhook URL, B5 runbook path). No code/schema/migration touched. D131 gate green throughout.

**S-DOCS-REORG-RESIDUAL** — SHIPPED 2026-05-13 (commits 02b2dc8 + 15540eb + this C3 commit — see PROGRESS.md SESSION LOG)
Outcome: THINGS_TO_DO_AT_THE_END.md moved root → docs/ via git mv (history preserved). Closes the 2026-05-13 DOCS-REORG residual / dirty-working-tree drag flagged at S-CHECKLIST-DRIFT-FIX close. No content changes; D131 gate green throughout.

**S-CHECKLIST-DRIFT-FIX** — SHIPPED 2026-05-13 (commits 63a594f + ec92f32 + this C3 commit — see PROGRESS.md SESSION LOG)
Outcome: 3 docs-only drift fixes against PREDEPLOY_CHECKLIST.md + CURRENT_SESSIONS.md. G2 invariant range bumped I1-I6 → I1-I10 with origin-session citations (I7-I10 added across S-MILEAGE-2A/2B/3/5). S-PROD-2 explicit QUEUED entry added with 3-piece scope (Sentry SDK + SES bounce webhook + key rotation runbook) — unblocks PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 reference accuracy. Active queue date stamp refreshed. No code/schema/migration changes; D131 gate green throughout.

**S-PREDEPLOY-CHECKLIST-CREATE** — SHIPPED 2026-05-12 (commits 7d5e62d + 2c5d146 + this C3 commit — see PROGRESS.md SESSION LOG)
Outcome: Created `FLEETFORGE_PREDEPLOY_CHECKLIST.md` as the canonical pre-deploy operations file. 21 backfilled obligations originally scoped (1 operator-specified A1 FF_ASSET_VERSION + 1 operator-specified F1 AR drift + 19 pre-existing scattered across SPEC_FINAL.md / REFERENCE.md / runbooks / .env) — expanded to 26 obligations across 8 active categories (A asset cache, B prod .env keys, C DNS, D AWS infra, F accounting state, G smoke, H rollback, I monitoring) + 1 empty placeholder (E data migrations) + 1 References section (J). D-B refinement: each item carries Originating session + Surfaced into checklist sub-fields. D-C refinement: "Original source:" pointer inside Detail. D-D refinement: bidirectional file-header cross-refs to K-14 + S-PREDEPLOY-CHECKLIST-CREATE. K-14 (Pre-deploy obligations category-separation discipline, K-12 extension) locked in PROGRESS.md KEY LEARNINGS. Cross-refs added to REFERENCE.md §0 LOCKED DECISIONS + §1 SEVEN FILES "Deployment-time companion" note; AWS Lightsail cutover entry (this file) extended with pointer to checklist; Pre-flight check section (D136) extended with category-separation note.

**S-D136-COMMIT-DISCIPLINE** — SHIPPED 2026-05-11 (bundled into S-DOCS-CLUSTER-2026-05-11 — see PROGRESS.md SESSION LOG)
Outcome: D-A of S-DOCS-CLUSTER-2026-05-11. Extended D136 wording in REFERENCE.md §0 LOCKED DECISIONS with a new "**Registration commit requirement (locked 2026-05-11 via S-D136-COMMIT-DISCIPLINE):** IN-FLIGHT registration must be COMMITTED to main before any subsequent operation (file edit, DB write, branch creation). Working-tree-only registration does not count as registered..." clause; cites S-ACCT-FIX-A1 + S-DOC-STATUS-RECONCILE-CLOSE-FIXUP as origin; clarifies branch isolation fallback addresses collision avoidance, not state-divergence avoidance. This session exercised the discipline being locked — IN-FLIGHT registration committed standalone (bd8dbb6) before content edits.

**S-D135-REFERENCE-PROMOTE** — SHIPPED 2026-05-11 (bundled into S-DOCS-CLUSTER-2026-05-11 — see PROGRESS.md SESSION LOG)
Outcome: D-B of S-DOCS-CLUSTER-2026-05-11. Added D135 index row to REFERENCE.md §0 LOCKED DECISIONS after D134, before D136 (terse-pointer format matching D134's pattern). Renamed existing §13.8 subsection heading from "Three valid configurations (D132/D133 clarified, S-MILEAGE-ALLOWANCE-ZERO-FIX, 2026-05-07)" to "Three valid configurations (D135, S-MILEAGE-ALLOWANCE-ZERO-FIX, 2026-05-07 — clarifies D132/D133)" so the §0 cross-reference resolves. Added D135 to the §13.8 "See also" list. Body content was already present in §13.8 (pre-existing from S-MILEAGE-ALLOWANCE-ZERO-FIX); promotion was the labeling + index row.

**S-LOOKUP-RATES-NAMESPACE-COMPLETE** — SHIPPED 2026-05-07 (commit d83c4e3 — see PROGRESS.md SESSION LOG)
Outcome: Single docs commit closing the docs rigor gaps from the original S-LOOKUP-RATES-NAMESPACE arc. Three pieces: D134 architectural lock added to PROGRESS.md DECISIONS + REFERENCE.md §0 LOCKED DECISIONS (canonical contract: `equipment_type` columns store `equipment_templates.category`, not `name`); KNOWN ISSUE #102 closed with full trace + commit refs; new `## KEY LEARNINGS — searchable index` section introduced with K-1 entry capturing namespace-mismatch fix protocol. SESSION LOG row backfilled in S-DOC-STATUS-RECONCILE-CLOSE (gap surfaced by S-QUEUE-STATUS-RECONCILE diagnostic).

**S-D130-EXTENSION** — SHIPPED 2026-05-11 (bundled into S-DOCS-CLUSTER-2026-05-11 — see PROGRESS.md SESSION LOG)
Outcome: D-C of S-DOCS-CLUSTER-2026-05-11. Extended D130 in PROGRESS.md DECISIONS with: "**Symmetric rule applies to scope CONTRACTION (locked 2026-05-11 via S-D130-EXTENSION):** if mid-session it becomes clear an originally-scoped item is out of scope, already shipped, or being deferred to another session, the contraction also requires explicit operator re-authorization + SESSION LOG diff capturing original-scope vs final-scope. Silent omission of scoped items is equally a discipline failure as silent addition." Origin cites S-MILEAGE-RATE-ZERO-FIX C1 silently dropping smoke gate work + INV-27/INV-84 voids. Treated as parallel promote-with-extension since D130 had zero REFERENCE.md presence pre-session — added §0 LOCKED DECISIONS row carrying combined expansion + contraction wording.

### UX session set (from real-use testing 2026-05-07)

**S-INVOICE-DISPLAY-COMPREHENSIVE** — SHIPPED 2026-05-11 (commits 8bc4e5c + aabb087 + 5dc2af2 + df1bd20 + f9130a2 — see PROGRESS.md SESSION LOG)
Outcome: 5-commit arc covering D-A through D-G + a T1-surfaced print-overlay fix. C2 (aabb087): backend data load extension — `$creditApplications` query against `credit_note_applications` mirroring `$invoicePayments` shape. C3 (5dc2af2): comprehensive line-item rendering with D-A draft-only $0 hide, dispatch contract comment block for S-MILEAGE-2A/2B extension, Credit Applications card between Line Items and Financial Summary (D-C), step-by-step Financial Summary with taxable/non-taxable split + "RATE% on $BASE" inline tax base + per-tax tooltip (D-B + D-D), dual S-MILEAGE-2B extension markers (HTML + PHP). C3 refinement (df1bd20): Subtotal label adapts to tax shape — plain "Subtotal" for single-class invoices, "(taxable lines) / (non-taxable lines)" only when both exist. C3-T1-FIX (f9130a2): global @media print hide list extended with `.search-modal, .search-overlay, .modal-overlay, .modal-backdrop, .sidebar-overlay` after T1 surfaced a dark semi-transparent overlay in browser Cmd+P / Save as PDF rendering. Side-fix disclosed per D130: pre-existing tax-rate display bug (rendered "0.05%" instead of "5%" since rates are stored as decimal fractions) was incidentally corrected in C3 by adding ×100 conversion in the label. T1 verification: technical via PHP CLI auth-faked render + synthetic CN apply against INV-13 with BEGIN/ROLLBACK isolation (rollback verified clean); operator visual T1 PASS on the 3 reference invoices (INV-19 tax-exempt sent, INV-13 paid ON HST, INV-21 BC GST+PST + discount). KEY LEARNING K-13 locked: global include CSS as universal print concern. `.env` FF_ASSET_VERSION 1.0.25 → 1.0.26 (uncommitted; .env gitignored).

**S-INVOICE-CREATION-UX** — SHIPPED 2026-05-07 (commits 044ffef + 6feb94c + cdb59ca + C4 — see PROGRESS.md SESSION LOG)
Outcome: 4-commit arc covering all three issues. C1 classified Issue 1 as VALIDATION GAP (KNOWN ISSUE #103, queued S-MILEAGE-RATE-VALIDATION-FOLLOWUP). C2 wired period auto-fill on invoice create form (12-fixture node smoke 12/12 PASS). C3 added Generate Invoice button on lease profile (8-cell PHP truth-table smoke 13/13 PASS). C4 = tests + this entry. Both new permanent smoke tests committed at tests/_smoke_invoice_create_period_autofill.js + tests/_smoke_lease_show_generate_invoice_button.php.

**S-UNIT-STATUS-COLOR** — SHIPPED 2026-05-14 (commits 9385ad7 C1 IN-FLIGHT + f3fc46e C2 helper + admin badges + drift fixes + SPEC fold-in + this C3 docs — see PROGRESS.md SESSION LOG row)
Outcome: Color status indicators applied across 3 admin surfaces (lease show, reservation show, maintenance work order show) using a new canonical PHP helper `unit_status_badge_class()` at `includes/functions.php` (mirroring equipment_health_color from S-CRON-3). Universal search equipment block routes through the same helper. DESIGN_DETAILS.md §9 6-status mapping locked: available=success / reserved=purple / on_lease=info / maintenance=warning / inactive=neutral / decommissioned=danger. Pre-flight surfaced 4 prompt divergences (5-status assumption with stale names; 2 surfaces already shipped; helper location convention; portal info-boundary concern) — all resolved via operator-confirmed AskUserQuestion. Two drift-fix sub-findings landed in scope: (a) `app/admin/reservations/show.php:1101` JS mapping had reserved→warning + maintenance→danger + decommissioned→neutral (now purple/warning/danger per canonical); (b) `api/v1/search.php` $badge_class closure had maintenance→info + inactive→info + reserved→warning for equipment rows — equipment results now route through shared helper, other entity types keep the multi-entity closure. SPEC_FINAL.md:864 stale "deferred to S-PORTAL-MILEAGE-MODEL-B" forward-pointer folded in: "shipped via S-PORTAL-MILEAGE-MODEL-B 2026-05-13" (carry-forward from S-QUEUE-STATUS-VERIFY). OUT OF SCOPE per operator: equipment/index.php + equipment/show.php main render (already shipped); invoices/show.php (snapshot field, not live); portal/leases/view.php (D10 info-boundary). NO CSS changes; NO FF_ASSET_VERSION bump; NO A4 entry; NO schema/migration motion. D131 gate clean on every commit.

### Bug investigation outcomes

**INV-2026-00090 mileage charge investigation** — RESOLVED via S-MILEAGE-ALLOWANCE-ZERO-FIX (2026-05-07)
Outcome: VALIDATION GAP classification (S-INVOICE-CREATION-UX C1) was reframed by operator as VALID-CONFIGURATION-NOT-RECOGNIZED. Lease 52's shape (rate=$0.18, allowance=0) is treated as Model B Lite ("bill every km from km 0") rather than a data hole. Engine fix in S-MILEAGE-ALLOWANCE-ZERO-FIX C1 (commit 2168bd5) restructured `$mileageBillingExpected` to key on `mileage_rate_km > 0`. INV-2026-00090 voided + regenerated as INV-2026-00091 with mileage line $91.26 + HST $11.86, total $2803.37 in C3 (commit ef050e7). KNOWN ISSUE #103 closed.
Side-findings closed: (a) duplicate-period draft INV-2026-00089 voided in C3; (b) period overshoot resolved by S-INVOICE-CREATION-UX C2 form auto-fill cap.

**S-MILEAGE-RATE-VALIDATION-FOLLOWUP** — SUPERSEDED 2026-05-07 by S-MILEAGE-ALLOWANCE-ZERO-FIX
Reason: original framing assumed defensive validation (reject the rate>0 + allowance=0 shape, queue 13-lease backfill). Operator reframed as engine-fix (admit the shape as Model B Lite, reframe Mileage::periodExcess to handle allowance=0 natively). The four planned layers reduced to one engine-guard restructure plus an I6 smoke invariant — no API rejection, no data backfill needed (12 of 13 affected leases had no exposed invoices; only lease 52's INV-90 needed regen). See S-MILEAGE-ALLOWANCE-ZERO-FIX SESSION LOG entry for full trace.

**S-REVIEW-MILEAGE-TAX-FIX** — SHIPPED 2026-05-12 (commits 3582e78 + 79934a0 + b1adbcd + docs — see PROGRESS.md SESSION LOG)
Outcome: 4-commit arc (1 IN-FLIGHT registration + 1 fix + 1 INV-92 remediation + 1 docs). C2 replaced the buggy `bcdiv(..., '100', 2)` wrapper at api/v1/invoices/review_mileage.php:213-218 with the canonical `bcround(bcmul(..., 6), 2)` pattern matching TaxCalculator.php:62; added WHY comment block referencing TaxCalculator.php:62 from the calling site; new stress test tests/_stress_review_mileage_tax.php (14 cases across canonical formula + source-pattern regression sentinel; pre-fix 9/14, post-fix 14/14 PASS exit 0). Pre-work scan #7 (full SELECT for ALL `mileage_adjustment` line items per K-15 discipline) surfaced INV-2026-00092 line 175 ($699.84 base, line_hst=$0.90 → $90.98) — created post-C5 audit-pass and missed by inv91_tax_correct_2026_05_07.php. C3 single-transaction audited UPDATE in scripts/inv92_tax_correct_2026_05_12.php mirrored C5's inv91 pattern (--dry-run/--execute + idempotent guard). Visible impact: line tax_hst $0.90 → $90.98 (delta +$90.08); invoice tax_hst_amount $106.21 → $196.29; tax_total $107.11 → $197.19; total_amount $1623.95 → $1714.03; balance_due $1623.95 → $1714.03; lease 52 total_invoiced += $90.08. K-15 locked (data-path coverage discipline). D131 gate clean on every commit.

### Mileage refactor arc (Model B — Avi's preferred billing model)

**S-MILEAGE-2A** — SHIPPED 2026-05-12 (commits b602bd9 IN-FLIGHT + 253b294 C2 activate.php balance init + e1918df C3 InvoiceGenerator precharge emit + (b) gate + engine markers + c8e459a C4 send.php stamp + 409 + 365d541 C5 T14/T15/T16 smoke ADD + b16c0fc C6 I7 invariant + 8d17548 C7 docs + this T1 PASS addendum — see PROGRESS.md SESSION LOG)
Outcome: Full Model B Invoice 1 lifecycle landed end-to-end — precharge_balance init on lease activation (D137), `mileage_precharge` line emitted on Invoice 1 generation under the 3-clause gate (D138: lifecycle + (b) cross-invoice uniqueness + billing_type exclusion), precharge_invoiced_at stamp on Invoice 1 send (D140 happy path) + 409 PRECHARGE_ALREADY_BILLED backstop for duplicate-send protection (D140 unhappy path), I7 invariant for precharge-tier rate-tier completeness (D143 / D132 extension). Decisions D137-D146 (D-A through D-J) locked. Mid-session clarifications surfaced + operator-approved: (b) cross-invoice uniqueness gate at C3 emission site paired with C4 send-time 409 (belt + suspenders defense-in-depth); ADD-not-REPLACE for T14/T15/T16 (preserves T8/T10/T12 placeholders as 2B carry-forward per REFERENCE.md §13.6 framing). All 5 stress tests + 16/16 smoke + I1-I7 invariants + parity + migrate clean on every commit. **T1 visual sign-off (5-step walk per D146): ✓ PASS — operator-confirmed 2026-05-12.**

**S-MILEAGE-2B** — SHIPPED 2026-05-12 (9.5-commit arc: 7acde72 C1 IN-FLIGHT + d4923c7 C2 ENUM/dispatch + a24cb49 C3 drawdown emit + Samsara + 64b37cb C3.5 silent-bug cleanup + 840c71f C4 Model C retirement + INV-87 void+regen + 6ed9529 C5 send gate retirement + f7b7e87 C6 panel/breakdown + 6dd449c C7 I8/T8/T10 + 47acbb9 C8 spec rewrite + this C9 docs commit — see PROGRESS.md SESSION LOG)
Outcome: Full Model B drawdown lifecycle on subsequent invoices end-to-end + Model C plumbing wholesale retirement. C3 lands InvoiceGenerator drawdown emit replacing the S-LEASE-MILEAGE per-period excess block (lines 559-713 wholesale) with 3-clause drawdown math (POSITIVE amount + is_credit=1 K-16 convention per D166 — locked D-B "negative amount" wording was abstract financial intent, codebase column convention at InvoiceGenerator.php:357-362 is canonical) + Samsara getDistanceForPeriod fallback fetch when caller doesn't pre-populate AND lease equipment_unit has samsara_vehicle_id. C3.5 fixes a silent JOIN bug (samsara_vehicle_id lives on equipment_units not leases — operator-authorized cleanup; K-18 KEY LEARNING locked: "stress test pre-population can hide bugs in fallback paths"). C4 retires 7 Model C columns (excess_distance_km, excess_charge_amount, mileage_review_status, mileage_override_amount, mileage_reviewed_at, mileage_reviewed_by_user_id, mileage_review_notes) + residual idx_mileage_review index via migration 202605120907 with backup table invoices_model_c_backup_S_MILEAGE_2B per D107; INV-87 void+regen produced INV-2026-00094 at $1370.52 mileage_usage line (was $1280.71 Model C draft — operator-confirmed amount delta, no D14 conflict since INV-87 never sent); Mileage::periodExcess DELETED; Mileage::monthlyAllowance RETAINED pending S-PORTAL-MILEAGE-MODEL-B refactor (D-H refined post-pre-C4-scan per D154 + D167); I6 invariant refactored for Model B with created_at >= MODEL_B_SHIP_DATE scope filter. C5 deletes api/v1/invoices/review_mileage.php (316 lines) + HARD send gate (D84) + show.php Mileage Review card markup + Alpine modal state. C6 lands the Drawdown Reconciliation panel + Odometer card rewrite (Period Charge row + Lease-to-Date Distance label + Samsara warnings banner) + Financial Summary 3-row breakdown render (Mileage usage / Precharge credit applied / Net mileage charge) at the S-INVOICE-DISPLAY-COMPREHENSIVE C3 HTML marker. FF_ASSET_VERSION 1.0.26 → 1.0.27 dev .env; filed under PREDEPLOY_CHECKLIST.md A2 per K-14. C7 lands I8 invariant (drawdown math sanity: credit == min(usage, pre_balance) AND post == pre − credit) + 6-case stress test using inline-predicate execution + T8/T10 real fixture-mode coverage replacing source-inspection placeholders. C8 rewrites FLEETFORGE_SPEC_FINAL.md Invoice Generation Flow + Invoice Calculation Order + Mileage Billing sections (Model A → Model B; precharge + drawdown only per D-M (ii); close-refund deferred to S-MILEAGE-3 spec rewrite). C9 docs: 21 DECISIONS rows D147-D167 (D-A through D-S + K-16 emit-shape clarification D166 + D-H refinement D167) + K-18 KEY LEARNING + REFERENCE.md §13.4 retirement (Model C → Model B) + §13.4.1 lifecycle table update + §13.6 T8/T10 status flip. **D131 gate clean on every commit:** PARITY OK + INVARIANTS OK (I1-I7 through C6, I1-I8 from C7) + samsara distance 16/16 + migrate 11 → 13 ok / 0 drift / 0 missing. **NOT TOUCHED:** api/v1/leases/close.php priorExcessKm safeguard (D-J / D156, S-MILEAGE-3 owns); FLEETFORGE_ACCOUNTING_SPEC.md (D-N DEFERRED per D160, S-MILEAGE-3 owns); lease_billing_periods table (load-bearing per K-15); lease_close_adjustments table (S-MILEAGE-3 territory). **T1 visual walk pending operator** per D146 pattern (8-step walk: 1) activate fresh precharge lease, 2) confirm precharge_balance init, 3) generate Invoice 1 → mileage_precharge line, 4) send Invoice 1 → stamp + balance unchanged, 5) re-edit lease post-send → PRECHARGE_LOCKED 409, 6) generate Invoice 2 → mileage_usage + mileage_drawdown_credit emit + balance decrement, 7) view Invoice 2 show page → Drawdown Reconciliation panel + Financial Summary breakdown render, 8) confirm no Mileage Review card + no Review Required label).


**S-MILEAGE-3** — SHIPPED 2026-05-13 (8-commit code arc + C9 docs commit: 6fcb859 C1 IN-FLIGHT + 1c57952 C2 credit_notes.source ENUM ext + c045fc4 C3 close.php refund dispatch + mark_refund_settled.php + 2c727c4 C4 close UI picker + Mark Refund Settled button + dfb8b23 C5 priorExcessKm retirement + lease_close_adjustments DROP + 721fac1 C6 Smoke I9 + stress + ff07f31 C7 FLEETFORGE_SPEC_FINAL.md close-refund rewrite + C8 SKIPPED (D-I CPA-blocked) + this C9 docs commit — see PROGRESS.md SESSION LOG)
Outcome: Model B close-refund lifecycle complete + Model C plumbing fully retired. C2 ENUM extension adds `precharge_refund` to `credit_notes.source` per D126 (migrate 13 → 14). C3 (load-bearing) extends `api/v1/leases/close.php` with refund dispatch block (gate: precharge_enabled=1 AND precharge_balance > 0) + 409 PRECHARGE_REFUND_LOCKED state machine + 422 PRECHARGE_REFUND_REQUIRED required-block gate; credit branch creates `credit_notes` row with `source='precharge_refund'` via gap-free counter + calls `AutoEntryBridge::onCreditNoteIssued` + stamps `settled_at=NOW()` at close-commit (D-E ii — credit issuance IS settlement); cash branch writes `method='cash'` with `settled_at=NULL` (D-B i deferred-settle); audit_log entity_type=`lease_precharge_refund_issued` per D-L (D102 workaround). New endpoint `api/v1/leases/mark_refund_settled.php` (FOR UPDATE on lease; 422 INVALID_LEASE_STATUS / METHOD_MISMATCH; 409 ALREADY_SETTLED idempotent; audit_log entity_type=`lease_precharge_refund_settled`). C3 inline checkpoint verifications: V1 precharge_balance NOT zeroed post-refund (preserve historical at-close value for audit trail; D182); V2 canonical closed-status confirmed as `'completed'` (D183 — leases.status ENUM has no 'closed' value); V3 refund dispatch atomicity confirmed inside db_transaction closure (D184). C4 close UI (commit 2c727c4) lands Precharge Refund picker in close modal (conditional render; cash/credit radio + default 'credit' + manager-notes textarea + "Model B" badge) + "Mark Refund Settled" btn-warning on lease show page precharge row + closeForm state ext + closeLease() payload assembly + markRefundSettled() Alpine method; FF_ASSET_VERSION dev .env bump 1.0.26 → 1.0.28; PREDEPLOY_CHECKLIST.md A3 entry per K-14. C5 (commit dfb8b23) retires priorExcessKm transitional safeguard from S-MILEAGE-FIX-0 D98 wholesale (~340 LOC across close.php + ~240 LOC across app/admin/leases/show.php) + DROPs `lease_close_adjustments` table via migration `202605121925_S-MILEAGE-3_close_adjustments_drop.sql` (backup table captures 0 rows per K-15 scan) + closeReconciliation Alpine getter retirement + master file CREATE TABLE swap; **latent bug surfaced + fixed**: `api/v1/leases/show.php` still queried `SUM(excess_distance_km) FROM invoices` but column was DROPPED in S-MILEAGE-2B C4 — caught + cleaned per K-15 READ-coverage extension. C6 (commit 721fac1) adds I9 invariant (closed lease + residual balance → non-NULL method + settled_at for credit branch; cash-branch NULL settled_at NOT a violation per D-B i defer-settle) + 6-case fault-injection stress test `tests/_stress_refund_state_machine.php` (6/6 PASS via inline-predicate pattern from S-MILEAGE-2B C7 D161). C7 (commit ff07f31) rewrites FLEETFORGE_SPEC_FINAL.md close-refund section (replaces S-MILEAGE-3 — pending placeholder with full SHIPPED behavior across all 5 sections: Lifecycle phase header + FINAL INVOICE block + Lease close + refund section + inline pseudocode + mileage_credit ENUM "closed category" → "DELETED CATEGORY"). **C8 SKIPPED — D-I CPA-blocked** (no CPA signal in session prompt; FLEETFORGE_ACCOUNTING_SPEC.md NOT touched; 5 CPA questions remain documented in D-I; S-MILEAGE-3-ACCT-SPEC follow-up session opens when accountant answers come in). C9 docs: this entry + 17 DECISIONS rows D168-D184 (D-A through D-N + 3 inline-verification rows V1-V3) + REFERENCE.md §13.4 close stage flip + §13.4.1 lifecycle table flip + "What S-MILEAGE-3 must do" → SHIPPED summary + CURRENT_SESSIONS.md SHIPPED flip + Recent ship history 2026-05-13 row. **D131 gate clean on every commit:** PARITY OK + INVARIANTS OK (I1-I8 through C5, I1-I9 from C6) + migrate 13 → 15 ok / 0 drift / 0 missing. **NOT TOUCHED:** Mileage::monthlyAllowance helper (S-PORTAL-MILEAGE-MODEL-B owns final deletion); customer portal app/portal/leases/view.php (S-PORTAL-MILEAGE-MODEL-B); lease_billing_periods table (load-bearing per K-15); payments table schema (D-B (i) lock avoids payments-table changes); FLEETFORGE_ACCOUNTING_SPEC.md (C8 SKIPPED per D-I (A)). **T1 visual walk pending** per D-M pattern (8-step walk: K-19 persistent user; covers cash branch Steps 1-7 + credit branch Step 8 + Model C absence verification).

**S-MILEAGE-3-ACCT-SPEC** — QUEUED
Scope: Write the FLEETFORGE_ACCOUNTING_SPEC.md precharge lifecycle section (activation liability, drawdown revenue recognition, cash refund JE per D-B (i) settle-time stamp, credit refund JE per D-C close-commit stamp, tax reversal on refund). Single-commit session landing after the CPA conversation produces answers to the 5 enumerated questions in S-MILEAGE-3 spec D-I (a-e).
Effort: ≤30 min, 1 commit.
Dependencies: S-MILEAGE-3 SHIPPED + CPA conversation complete.
Blocked on: accountant input (D-I 5 questions: GL accounts, recognition timing, cash refund AP-vs-direct-JE, credit refund AutoEntryBridge integration, tax handling on refund).
Discussed: 2026-05-12 in planning chat (S-MILEAGE-3-SPEC-LOCK).

**S-MILEAGE-5** — SHIPPED 2026-05-13 (commits 7dec061 C1 IN-FLIGHT + e16d3f6 C2 tests + this C3 docs commit — see PROGRESS.md SESSION LOG)
Outcome: 20-scenario hermetic Model B lifecycle test file shipped at `tests/_smoke_model_b_lifecycle.php` (S01–S20 covering Invoice 1 lifecycle, drawdown math at three balance/charge boundary shapes, multi-invoice exhaustion + bypass, all three close-refund cases per K-21, mark-settled happy path + state-machine 409s + 422 validators via inline-predicate mirrors, $0.01 bcmath precision edge, Model B Lite + Disabled mileage D135 rows, K-18 Samsara fallback discipline, FIX_RESET fixture-mode warning capture, and S20 end-to-end I10 cross-check). I10 invariant added to `tests/_smoke_billing_invariants.php` (count 9→10) with credit + cash branch coverage; closes the K-21 future-I10 candidate (locked here 2026-05-13). 20/20 PASS exit 0; INVARIANTS OK I1–I10; samsara 16/16; refund + drawdown stress 6/6 + 6/6 (no regression); migrate 15/0/0. **No production code, schema, or migration changes** — pure test + docs session against the surface frozen from S-MILEAGE-3-FIX-0. No T1 walk this session (visual verification completed in S-MILEAGE-3-FIX-0 T1 9/9 PASS on 2026-05-13). Model B mileage refactor arc fully closed pending S-MILEAGE-3-ACCT-SPEC for CPA-blocked accounting spec text.

(S-MILEAGE-4 placeholder may dissolve into 2B/3 — remove from queue when confirmed.)

### Architectural follow-ups (lower priority but real)

**S-USERS-CONSOLIDATE** — SHIPPED 2026-05-15 (commits 9900d86 C1 + c6df5bd C2 Team enrichments + de763f4 C3 Portal tab + detail + 6 API endpoints + 4a58284 C4 Settings link card + this C5 docs — see PROGRESS.md SESSION LOG)
Outcome: unified user management under the sidebar Users module — two tabs ("Team" + "Portal Users" per D-A). Team tab enriched per D-C: MFA 3-state pill, colored role badge, relative last-login time, invited-row muted-italic styling, KPI tiles refreshed to Total Team / Active / Pending Invite / MFA Enrolled%. Portal Users tab built with sortable list, KPI tiles (Total / Active / Pending / Email Disabled), inline Create form, customer-link column, Primary + Email Off badges, deep-link via `?tab=portal`. New `app/admin/portal_users/show.php` detail page per D-B (left-rail detail + right-rail actions + recent login activity). 6 new API endpoints under `api/v1/portal_users/` (index, create, update_status, reset_password, delete, reenable_email — the reenable_email closes the S-PROD-2 portal-side gap that didn't exist before). Settings → Portal Users tab swapped to centred link card matching the S-SETTINGS-CLEANUP D195 pattern; settings/portal_users.php kept on disk with DEPRECATED docblock header per D200 (D-D). 5 DECISIONS locked D197-D201. Closes the deferred decommission left open by S-SETTINGS-CLEANUP D195. D131 gate clean throughout (no schema motion; 19/0/0 sticky).

**S-MILEAGE-HELPERS-CLEANUP** — SHIPPED 2026-05-14 (commits 27b5f32 C1 IN-FLIGHT + 6ba4dbd C2 deletions + this C3 docs — see PROGRESS.md SESSION LOG row)
Outcome: 3 orphaned dead helpers (leaseDurationMonths + toDisplayUnit + formatDistance per S-PORTAL-MILEAGE-MODEL-B D-E) + 3 orphan class constants (MONEY_SCALE, OPEN_ENDED_FALLBACK_MONTHS, DISTANCE_SCALE — operator-confirmed clean-cleanup scope expansion via AskUserQuestion at pre-flight) deleted from `lib/Billing/Mileage.php`. K-15 verified zero external callers pre + post deletion. Class shell retained as tombstone (file-level retirement-history docblock + empty class body) so stale `use` references get class-exists shape rather than class-not-found fatal. **Model B mileage arc fully closed** on production-code surface: engine (S-MILEAGE-1 → -2A → -2B → -3 → -3-FIX-0 → -5) + portal (S-PORTAL-MILEAGE-MODEL-B) + helper retirement (this session). Pending: S-MILEAGE-3-ACCT-SPEC (CPA-blocked, independent of code path). No behavioral change; no schema/migration/CSS motion; D131 gate green throughout.


**S-TEMPLATE-MILEAGE-DEFAULTS** — SHIPPED 2026-05-13 (commits 32dc6f8 C1 IN-FLIGHT + b962f6a C2 migration + master mirror + this C3 docs commit — see PROGRESS.md SESSION LOG)
Outcome: 3-commit arc landing NOT NULL DEFAULT 0.0000 enforcement on `equipment_templates.default_mileage_rate` with precision widening 8,4 → 10,4 to align with the `leases.mileage_rate_km` / `mileage_rate_miles` convention (S-LEASE-UNITS, master file lines 2171-2172). **Pre-work scan confirmed zero NULLs** at session start — S-MILEAGE-RATE-ZERO-FIX (2026-05-06 per S-REEFER-RATE-AUDIT finding) had already backfilled production templates 1/3/4/5 (id=1 $0.2897 custom; id=3 $0.1700 / id=4 $0.1500 / id=5 $0.1300 all rubric-aligned), and the 7 zero-default seed templates (IDs 6/7/10/11/12/14/17) all have zero active leases per P3 — intentional non-billable types per D135 config 3 (rate=0 → mileage disabled). No backfill executed; migration is the schema-tightening step only. Migration `202605122229_S-TEMPLATE-MILEAGE-DEFAULTS_not_null_default.sql` includes a `ff_assert_no_null_mileage_rates()` pre-flight SIGNAL guard for replay safety. **D131 gate clean on every commit:** PARITY OK + INVARIANTS OK I1-I10 + migrate 16 ok / 0 drift / 0 missing + post-apply `SHOW COLUMNS` confirms `decimal(10,4) Null=NO Default=0.0000` + post-apply zero NULLs. **Renders S-LOOKUP-RATES-PRODUCTION-INVARIANT (QUEUED optional) redundant** — the "template default must be non-NULL" branch of the invariant is permanently trivially true under the new schema; SUPERSEDED in this file below. **Side-fix:** removed 4 stray tool-call closing-tag artifacts at `docs/FLEETFORGE_PROGRESS.md:167-170` left in the SESSION LOG section by the prior S-REEFER-RATE-AUDIT C2 edit (broke markdown table rendering between the last row and the `---` separator).

**S-LEASE-RATE-AMENDMENT** — SHIPPED 2026-05-13 (4-commit arc: bf16f10 C1 IN-FLIGHT + e727c9b C2 amend_rate endpoint + update.php block + ea16bd5 C3 show.php UI + this C4 docs commit — see PROGRESS.md SESSION LOG)
Outcome: 4-commit arc (contracted from prompt's 5-commit arc per D130 — see SESSION LOG for diff). Closes the "rate fields immutable, amendment workflow not implemented" gap with NEW `api/v1/leases/amend_rate.php` endpoint + structured rate-amendment modal on lease show page + explicit rate-field BLOCK in `api/v1/leases/update.php`. **Operator-locked D-A through D-D:** D-A prospective only (sent invoices immutable per D14); D-B any time while status='active'; D-C any admin (leases:edit), no approval gate; D-D no auto credit notes for sent invoices. **Operator decisions surfaced via AskUserQuestion + locked mid-session:** (1) **Table strategy: option (A)** reuse existing `lease_amendments` table with amendment_type='rate_change' rather than create a parallel `lease_rate_amendments` table per the prompt's literal spec — DROPS the migration commit entirely, no schema change, automatic UI integration via the existing Amendments tab. (2) **gps_cost handling:** keep mutable via update.php per S-LEASE-GPS-COST, also amendable via amend_rate.php (two paths). (3) **rate_method handling:** drop entirely — no such column exists on `leases` (runtime concept in InvoiceGenerator). Pre-work surfaced 3 prompt schema mismatches: no `rate_method` column, no `version` column (D19 uses updated_at), gps_cost already mutable (S-LEASE-GPS-COST). **D131 gate clean on every commit:** PHP lint clean on amend_rate.php / update.php / show.php; doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 17/0/0 (UNCHANGED — option (A) means no new migration). Stress (a) verified via BEGIN/ROLLBACK CLI; stress (b)/(c)/(d) verified by code-inspection. **Tier 3 complete.** Remaining queue items: S-MILEAGE-3-ACCT-SPEC (CPA-blocked) + S-MILEAGE-HELPERS-CLEANUP (optional defer). No production .php beyond the 3 touched files; no migration; no schema; no `.env` / FF_ASSET_VERSION change.

**S-LEASE-21-CLEANUP** — SHIPPED 2026-05-13 (commits a8498ea C1 IN-FLIGHT upgrade + this C2 data + docs — see PROGRESS.md SESSION LOG)
Outcome: 2-commit arc. C1 IN-FLIGHT upgrade RO→write per S-D136-COMMIT-DISCIPLINE. Investigation confirmed lease 21 (CN-7B5Z5B-2026) is seed/test data — real activate→close delta = **1 min 13 sec**, not 3 min (audit_log #93→#94→#95); customer 3 Lepore Enterprise (780-555-0789 fictional 555 prefix, Phase 0.5 counter-drift culprit per KI #51); zero payments ever recorded; mileage_rate_km=0, precharge_enabled=0; no billable activity expected. **Disposition (C): seed/test data, no real customer impact.** C2 data corrections: INV-93 (id 162) draft → void + soft-delete (test artifact created 2026-05-11); lease 21 outstanding_balance $250.00 → $0.00 (residual drift correction); audit_log row id=1040 entity_type='lease_counter_correction' (S-FIX-2 pattern). C2 docs: KI #107 consolidating A2 + A4 with full disposition + references (NOT the #93 in the prompt sketch — #93 was taken by S-PROD-2 audit #18); RESOLVED markers appended to 4 embedded A2/A4 refs (3 in S-MILEAGE-RATE-VALIDATION SESSION LOG row + 1 in D133 trailing line); SESSION LOG row. **D131 gate clean:** PARITY OK + INVARIANTS OK I1-I10 (10/10) + migrate 15/0/0 + DB checks (lease 21 OB=0.00, zero non-void invoices on lease 21). **NOT touched:** any .php file, any schema, any migration, customer 3 outstanding_balance (corrected in S-FIX-2 already), INV-26/27/84 (already void from prior sessions). Lease 21 left in `completed` state (not soft-deleted — state is accurate, no FK cascade risk).

**S-REEFER-RATE-AUDIT** — SHIPPED 2026-05-13 (commits f0a5599 C1 IN-FLIGHT upgrade + this C2 file + docs — see PROGRESS.md SESSION LOG)
Outcome: 2-commit arc. Investigation (I1-I7) confirmed `scripts/seed_rate_cards.php` was NEVER run against the production DB (I7: no "Standard 2025" or "Reefer Specialist 2025" cards exist — only 4 customer-specific cards LP Logistics / Puar Transport / Lepore USD / Avi Owner-Op, all `is_default=0`). Seed rubric reefer = $0.22/km (industry-standard with cold-chain premium); live data: equipment_templates id=2 "48ft Reefer" default = $0.1800; customer_equipment_rates + rate_card_items range $0.15-$0.25 across 4 customers; only ONE historical reefer mileage line item ever billed (INV-2026-00094 / lease 2 / RFR-001 at $0.18 from S-MILEAGE-2B C4 INV-87 regen). **Disposition (C): intentional divergence** — seed script reflects industry-standard reference rubric, live production data reflects Mainland T&T operator pricing decisions. C2 comment-only edits to `scripts/seed_rate_cards.php` (value $0.2200 unchanged): file-header @note block + 13-line inline comment above EQUIP_REEFER BASE_RATES row. No DB writes. No schema/migration changes. D14 active-lease-rate immutability preserved. D131 gate clean: `php -l` no syntax errors; PARITY OK; INVARIANTS OK I1-I10; migrate 15/0/0; grep markers verified.

**S-SEED-RATE-CARDS-LOAD** — SHIPPED 2026-05-13 (3-commit arc: f70ae52 C1 IN-FLIGHT + 5e53d8e C2 migration + this C3 docs commit — see PROGRESS.md SESSION LOG)
Outcome: Standard 2025 rate card loaded as `is_default=1` system-wide fallback via `db_migrations/202605130934_S-SEED-RATE-CARDS-LOAD_standard_2025_default.sql`. **Operator decision:** option (A) FULL rates per BASE_RATES (daily + weekly + monthly + mileage) for 5 main equipment categories — dry_van $125/$800/$2200/$0.18, reefer $145/$950/$3200/$0.18 (live per S-REEFER-RATE-AUDIT, NOT rubric $0.22), flatbed $120/$780/$2100/$0.17, container $95/$620/$1700/$0.15, chassis $80/$520/$1400/$0.13. Specialty types (lowboy / step_deck / tanker / dump / other) intentionally not populated — fall through to equipment_templates defaults at Priority 3 per current pricing policy. **Schema mismatch surfaced + adapted:** prompt's INSERT specified `customer_id NULL` but rate_cards has no customer_id column (existing 4 customer-named cards are name-labeled catalog entries, zero FK link to customers); migration adapted to actual schema (dropped customer_id, added required effective_from='2025-01-01' opening earlier than the 4 existing 2025-10-09 cards). Rate-lookup chain `api/v1/leases/lookup_rates.php:143` Priority 2 ORDER BY `rc.is_default DESC` confirms the new is_default=1 card wins over the 4 customer-named is_default=0 cards for any covered category. **Master file edit SKIPPED** per D127 parity discipline (standalone `--` comments don't survive mysqldump round-trip → would create drift; migration docblock + SESSION LOG row are the canonical documentation). Idempotency: rate_cards INSERT guarded by NOT EXISTS on (name + is_default + deleted_at); rate_card_items INSERTs guarded by NOT EXISTS on (rate_card_id + equipment_type); defensive ff_assert_standard_2025_id() SIGNAL halts cleanly on soft-delete race. Post-apply: SELECT COUNT(*) WHERE is_default=1 → 1; rate_card_items for Standard 2025 → 5 rows; existing 4 cards untouched. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + doc_freshness 17/17 + migrate 17 ok / 0 drift / 0 missing. NOT touched: any production .php, master schema, equipment_templates, customer_equipment_rates, the 4 existing rate cards, .env / FF_ASSET_VERSION, tests.

**S-EQUIPMENT-SHOW-RESPONSIVE** — SHIPPED 2026-05-05 (commits 110e6dd + 2294f09 — see PROGRESS.md SESSION LOG line 126)
Outcome: Responsive layout fixes for app/admin/equipment/show.php at 768px and 375px breakpoints. D-A through D-J locked + T1-T8 results + KEY LEARNINGS already inline in SESSION LOG entry at PROGRESS.md line 126. CURRENT_SESSIONS status was stale (still listed as QUEUED until S-DOC-STATUS-RECONCILE-CLOSE flipped it 2026-05-07) — gap surfaced by S-QUEUE-STATUS-RECONCILE diagnostic.

**S-DOC-FRESHNESS-DISCIPLINE** — SHIPPED 2026-05-13 (commits 342ec29 C1 IN-FLIGHT + 46ebb7e C2 smoke + REFERENCE.md gate extension + this C3 docs commit — see PROGRESS.md SESSION LOG)
Outcome: 3-commit arc landing `tests/_smoke_doc_freshness.php` (293 lines, 17/17 PASS exit 0) as the third D131 gate alongside parity + invariants + migrate-verify. Four classes of staleness detection: **Class 1** canonical file existence + readability + non-empty (7 checks, one per canonical doc per SEVEN FILES list — master SQL at repo root, not docs/); **Class 2** SESSION LOG / CURRENT_SESSIONS cross-consistency (regex extracts every SHIPPED **S-LABEL** entry, substring-checks PROGRESS.md presence — 17 SHIPPED labels in current state, all present; bundled-session-friendly via substring-not-row-strict match); **Class 3** tool-call markup leak scan (lines whose trimmed content equals one of 8 known fragments `</content>` / `</invoke>` / `<invoke>` / `<content>` / `</invoke></content>` / `</content></invoke>` / `<function_calls>` / `</function_calls>`; code-fence-aware so legitimate inclusions inside ``` blocks are NOT flagged — automates the manual fix S-TEMPLATE-MILEAGE-DEFAULTS C3 side-fixed at PROGRESS.md:167-170); **Class 4** D136 IN-FLIGHT discipline (≤1 write-mode IN-FLIGHT entry; `(?!-RO)` negative-lookahead so IN-FLIGHT-RO entries don't count toward the limit). Injection test verified the smoke fires (appended `</invoke>` to PREDEPLOY_CHECKLIST.md → exit 1 with line-number output; restored → exit 0). REFERENCE.md §13.7 See-also line 1237 extended to mention the new smoke alongside the original parity + invariants pair (the canonical D131 body in PROGRESS.md:294 preserved as-locked — extension wording captured via the See-also bridge + the SESSION LOG row, mirroring how D130 + D135 extensions were handled). **PROGRESS.md cleanup (originally-scoped Part B): NO-OP** — P1 grep confirmed zero standalone artifacts; the "Known targets (as of 2026-05-12)" PROGRESS.md:160-163 (shifted to 167-170 post-row-add) artifact set was already resolved by the S-TEMPLATE-MILEAGE-DEFAULTS C3 side-fix. D131 gate clean on every commit (doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 16/0/0 + PHP lint clean). NOT touched: any production .php, migration file, FLEETFORGE_DATABASE_MASTER.sql, SPEC_FINAL.md, ACCOUNTING_SPEC.md, PREDEPLOY_CHECKLIST.md, D131 body in PROGRESS.md DECISIONS, `.env` / FF_ASSET_VERSION.

**S-MILEAGE-1B-FOLLOWUP** — SHIPPED 2026-05-06 (commit b886d1c — see PROGRESS.md SESSION LOG)
Outcome: T13 fixture coverage for FIX_GAP scenario (large_gap_detected warning) — closed audit-pass PARTIAL on S-MILEAGE-1B Item 10. T13 added to tests/_smoke_samsara_distance.php asserting distance=='2300.00' / source=='gps' / warnings includes 'large_gap_detected'. Counts updated (12 → 13) across smoke docblock + banner + REFERENCE.md §13.6. SESSION LOG row backfilled in S-DOC-STATUS-RECONCILE-CLOSE (gap surfaced by S-QUEUE-STATUS-RECONCILE diagnostic).

**S-LOOKUP-RATES-PRODUCTION-INVARIANT** — SUPERSEDED 2026-05-13 by S-TEMPLATE-MILEAGE-DEFAULTS
Rationale: the invariant's third clause — "template default must be non-NULL" as the fallback when customer-equipment-rate AND rate-card-item lookups both return NULL — is permanently trivially true under the new NOT NULL DEFAULT 0.0000 schema on `equipment_templates.default_mileage_rate` (enforced via migration `202605122229_S-TEMPLATE-MILEAGE-DEFAULTS_not_null_default.sql`). There is no remaining defensive surface for the smoke to test; building it would assert a constraint MySQL itself now guarantees. The customer-rate / card-item lookup branches that the invariant was also intended to cover are exercised by `tests/_smoke_billing_invariants.php` I3 (no template rate-tier holes) and the rate-lookup integration paths in production code; the supersession does not leave a gap. SUPERSEDED 2026-05-13 by S-TEMPLATE-MILEAGE-DEFAULTS schema enforcement (commit b962f6a).

**S-INVOICE-CUMULATIVE-TOTAL-LABEL** — SHIPPED 2026-05-13 (combined-arc with S-INVOICE-BACKDATE-WARNING: 8a761a8 C1 IN-FLIGHT registration + this C2 verification + Bug 4 build + docs — see PROGRESS.md SESSION LOG)
Outcome: **Verification-only session** — D163 incidental fix in S-MILEAGE-2B C6 had already replaced the "Cumulative Total" label with "Lease-to-Date Distance" during the Odometer card rewrite. **V1** repo-wide grep across `app/ api/ lib/` for `Cumulative Total|CumulativeTotal|cumulative_total|cumulative-total` returned **zero hits** (clean). **V2** new label confirmed present at `app/admin/invoices/show.php:1576` (`<dt class="text-secondary">Lease-to-Date Distance</dt>`). **V3** smoke gates clean (doc_freshness 17/17 + invariants I1-I10 PASS exit 0). No code changes needed for this session — the rider's grep-confirmation scope (any other surface using the old label) is now closed. D131 gate clean.

**S-INVOICE-BACKDATE-WARNING** — SHIPPED 2026-05-13 (combined-arc with S-INVOICE-CUMULATIVE-TOTAL-LABEL: 8a761a8 C1 IN-FLIGHT registration + this C2 Bug 4 build + Bug 1 verification + docs — see PROGRESS.md SESSION LOG)
Outcome: **Bug 4 fix landed as TWO reactive advisory banners in `app/admin/invoices/create.php`** per operator-confirmed scope (C) covering both backdate shapes that surfaced during investigation. **Investigation finding:** the canonical Bug 4 definition locked at PROGRESS.md:562 (D163 rider) was "soft validation when `billing_period_start < lease.start_date`" — pre-contract-start warning — which differs from the prompt's expected "period_end < TODAY" framing. After surfacing both shapes, operator authorized **option (C) build both warnings** (covers pre-contract-start AND already-billed-period risks; two stacked amber banners; non-blocking advisories). **Implementation:** new `_todayYmd()` helper + `backdateWarnings()` reactive method returning `[{kind, text}]` array consumed by `<template x-for>` block placed immediately after the existing `periodWarning` template at line 137-140. Banner (a) `pre_lease_start` fires when `form.period_start < _leaseStartDate` (set in `onLeaseChange` from option `data-lease-start-date`). Banner (b) `past_period_end` fires when `form.period_end < _todayYmd()`. **NO server-side validation** added — soft signal only, operator may legitimately backdate (catch-up invoices, late-recorded periods). **D131 gate clean:** PHP lint clean on touched file; JS syntax clean on the added `backdateWarnings()` block (validated via Node Function-constructor parse); doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 16/0/0. **NOT touched:** any server-side endpoint, any migration, any schema, billing engine, FLEETFORGE_DATABASE_MASTER.sql, `.env` / FF_ASSET_VERSION, tests directory. Single .php file touched (`app/admin/invoices/create.php` UI extension only; no edit.php exists).

**S-PORTAL-MILEAGE-MODEL-B** — SHIPPED 2026-05-13 (3-commit arc: a5d03cd C1 IN-FLIGHT registration + 7d8932f C2 portal refactor + monthlyAllowance deletion + this C3 docs commit — see PROGRESS.md SESSION LOG)
Outcome: `app/portal/leases/view.php` Model C allowance card retired and replaced with Model B mileage section per D135 three-config matrix; `Mileage::monthlyAllowance` helper DELETED (D154 + D167 final retirement). **Pre-work scan (P1-P5):** P1 read view.php (current Model C card spans PHP block lines 72-134 + HTML block lines 268-355; renders for any lease with estimated_mileage_km>0). P2 confirmed `monthlyAllowance` had exactly one production caller at view.php:80. P3 confirmed the helper's deletion would also leave `leaseDurationMonths` + `toDisplayUnit` + `formatDistance` orphaned (zero callers each — internal-helper-now-dead-code). P4 confirmed precharge_* columns already accessible in portal via `l.*` SELECT — no schema change. P5 mapped Model B line item shape (`mileage_usage` km in `quantity`, `unit_price` = rate; `mileage_drawdown_credit` amount in `amount`; `mileage_precharge` amount in `amount`). **Operator confirmation via AskUserQuestion (D-A through D-E):** prompt-shape on all three configs (precharge / Model B Lite / hidden); last 5 invoices history depth; **defer orphan helper cleanup** per prompt's literal instruction (surfaced for a follow-up S-MILEAGE-HELPERS-CLEANUP sweep). **C2 surface:** Config 1 (precharge: rate>0 + precharge_enabled=1) renders "Mileage Precharge" card with three summary tiles (paid / credits applied to date / balance remaining) + close-state-aware refund block (credit applied with CN # / cash pending / cash issued) + drawdown history table (last 5 disclosed invoices: invoice # / period / distance / charge / credit applied). Config 2 (Model B Lite: rate>0 + precharge_enabled=0) renders "Mileage Usage" card with rate row + usage history table (last 5 disclosed invoices: invoice # / period / distance / charge). Config 3 (rate=0) renders nothing. History queries bounded to non-void disclosed statuses (`sent`/`partially_paid`/`paid`/`overdue`) per Trap 8 — customers never see drafts. **lib/Billing/Mileage.php:** monthlyAllowance method body + docblock deleted; tombstone comment in its place referencing D154 + D167 final retirement context. Class-internal helpers retained per D-E defer. **Post-deletion verification:** `grep -rnE "::monthlyAllowance|function monthlyAllowance" app/ api/ lib/` → zero hits; `grep -rnE "monthlyAllowance\s*\(" app/ api/ lib/` → zero hits; only matches are tombstone comment lines in lib/Billing/Mileage.php (documentation, not code). REFERENCE.md §13.4 portal-pending references flipped to ✓ DELETED at three sites (line 770, line 881, line 889). **D131 gate clean on every commit:** PHP lint clean on both touched files; doc_freshness 17/17 PASS exit 0; PARITY OK; INVARIANTS OK I1-I10 PASS exit 0; migrate `--verify` 16 ok / 0 drift / 0 missing. **NOT touched:** any billing engine, migration, schema, FLEETFORGE_DATABASE_MASTER.sql, admin-side invoice show/create (S-MILEAGE-2B / -3 already retired Model C plumbing there), tests directory, `.env` / FF_ASSET_VERSION. **Tier 2 complete** — Model B mileage refactor arc fully closed in production code (engine + portal + helpers) pending S-MILEAGE-3-ACCT-SPEC for CPA-blocked accounting spec text + a future S-MILEAGE-HELPERS-CLEANUP sweep for the three orphaned helper methods left in Mileage.php per D-E defer.

### Strategic / multi-session arcs (planning phase)

**QuickBooks integration** — PLANNING
Scope: bidirectional sync with QBO as source of truth for customers + payments, FleetForge as source for invoices. Implies deprecating most of FleetForge accounting module (S028-S037). Estimated 6-7 sessions: S-QBO-1 discovery, S-QBO-2 customer sync, S-QBO-3 invoice export, S-QBO-4 payment import, S-QBO-5 reconciliation tooling, S-QBO-6 accounting module deprecation, S-QBO-7 cutover.
Status: planning will happen in a separate claude.ai web chat with FLEETFORGE_QUICKBOOKS_SPEC.md as the dedicated canonical doc. Not yet started.
Dependencies: defer until after Model B refactor (S-MILEAGE-2A/2B/3) and accountant conversation.

**AWS Lightsail cutover** — ACTIVE (new chat)
Scope: production deployment to AWS Lightsail Oregon.
Pre-deploy obligations: `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md` (26 obligations across categories A-I) — categories C (DNS), D (AWS infra D1-D9 — Lightsail provision, snapshots, S3 bucket + versioning, mysqldump cron, SES sandbox exit, SNS topic, IAM, CloudWatch alarms), G (smoke G1-G4), H (rollback H1-H2), I (monitoring I1-I3). Each ITEM there carries Originating session + Action + Owner; flip Status → ✅ COMPLETE in the checklist as the cutover progresses. Discipline locked as K-14 (pre-deploy obligations file separate from KNOWN ISSUES and CURRENT_SESSIONS) — see PROGRESS.md KEY LEARNINGS.
Status: Tier 1/2/3 feature sessions complete as of 2026-05-13. New dedicated planning chat opened 2026-05-13 for the cutover arc. Working doc: `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md` (primary reference for the new chat). `FLEETFORGE_QUICKBOOKS_SPEC.md` (separate QBO chat, not yet started).

**S-PROD-2** — SHIPPED 2026-05-02 (see PROGRESS.md SESSION LOG row at line 104; DECISIONS D75-D78)
Outcome: production-readiness instrumentation complete. **Sentry SDK** at `lib/Observability/Sentry.php` with `init()` / `captureException()` / `captureMessage()` and `before_send` PII scrub for bcrypt `$2y$` hashes, `ENC:` AES ciphertext, 12 sensitive key names from request data / cookies / headers / stack frames (D-A no-op when `SENTRY_DSN` blank; D-B scrub locked as D75/D76). Wired into `api/bootstrap.php`, `includes/header.php`, `app/portal/includes/header.php`, all 19 cron files. **SES bounce webhook** at `api/v1/webhooks/ses_notifications.php` with `Aws\Sns\MessageValidator` signature check (403 on fail), SubscriptionConfirmation auto-confirm, permanent bounces + complaints → `email_disabled=1` on `customers` + `portal_users` (NOT a separate `email_suppressions` table — directly-on-row flag pattern per D77 design choice); transient bounces → `email_bounces` audit log only. `Mailer::isEmailDisabled()` pre-send guard (fails-open on DB exception). Manager re-enable via `api/v1/customers/reenable_email.php`. **Key rotation runbook** at `docs/runbooks/key_rotation.md` covering 5 secrets (FF_MFA_SECRET_KEY, AWS access keys, DB password, SES SMTP credentials, Cloudflare API token). Logrotate runbook at `docs/runbooks/logrotate_setup.md`. Staging deferred per D78 until first production month. Schema lives in `db_migrations/S-PROD-2_ses_bounce_handler.sql` (already applied; counted in current migrate 17). PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 reference accuracy flipped retroactively in S-PROD-2-DOCS-RECONCILE 2026-05-13 (after S-CHECKLIST-DRIFT-FIX C2 added a phantom QUEUED entry based on stale info).

**S-PROD-3** — SHIPPED 2026-05-14 (commits d3be343 C1 IN-FLIGHT + abbce70 C2 fonts + b101807 C3 ApexCharts + d07ed0c C4 Leaflet + Chart.js + CSP tighten + this C5 docs — see PROGRESS.md SESSION LOG)
Outcome: Self-hosted all 4 CDN-delivered front-end dependencies — eliminates external CDN calls at runtime. Google Fonts (DM Sans v17 + DM Mono v16, 12 woff2 files via @font-face in app.css), ApexCharts 3.45.1, Leaflet 1.9.4 (+ default marker images at images/ subdirectory matching CSS relative refs per D186), Chart.js 4.4.3 (pre-flight expansion — was not in prompt's 3-library scope but surfaced as 4th CDN dependency at reservations/index.php:1412; operator authorized inclusion per zero-CDN goal). 22 PHP files touched: 17 with Google Fonts <link> blocks removed + 4 with CDN <script>/<link> swapped to asset_url() per D27 + 1 (includes/footer.php) for ApexCharts. CSP tightened in public/.htaccess: removed cdn.jsdelivr.net + fonts.googleapis.com + fonts.gstatic.com from script-src/style-src/font-src per D187. Vendor layout `public/assets/vendor/{fonts,apexcharts,leaflet,chartjs}/` mirrors existing `vendor/alpinejs/` precedent (D185). READMEs in each vendor subdir document source/version/license/consumers/update procedure. FF_ASSET_VERSION 1.0.28 → 1.0.29 dev .env (operator) + A4 entry in PREDEPLOY_CHECKLIST.md per K-14 for C2 Google Fonts CSS change. 3 DECISIONS rows locked: D185 vendor layout convention; D186 Leaflet marker-image strategy; D187 CSP tightens in lockstep with CDN removal. Post-ship grep for `cdn.jsdelivr|fonts.googleapis|fonts.gstatic|unpkg|cdnjs` across `app/ + public/ + includes/` returns zero runtime refs. No schema/migration motion; D131 gate green throughout.

---

## Recent ship history (rolling — older entries archived to PROGRESS.md)

**2026-05-19:**
- S036 SHIPPED (single-commit arc — see PROGRESS.md SESSION LOG row) — Phase B Financial Reports + Budget module + AI integration. **Part A** `lib/Accounting/ReportingService.php` (NEW, ~700 lines): `profitAndLoss($from, $to, $compareMode, $budgetId)` with prior_period / prior_year / budget comparison + budget pro-rating to partial date ranges; `balanceSheet($asOf, $compareMode)` with YTD-net-income equity injection + is_balanced check; `cashFlow($from, $to)` ASPE 1540 indirect method with non-cash adjustments + working capital movement + investing/financing sections + GL 1010 tie-out; `assetSchedule($asOf, $category)` PP&E continuity by asset_class with per-asset drill-down. **Part B** `lib/Accounting/BudgetService.php` (NEW): create/update/delete/copy/variance with `dec`-column backticking, D19 optimistic-lock, copy-prior-year flag, day-share pro-rating for partial-range variance. **Migration** `db_migrations/202605190030_S036_ai_summaries_accounting_types.sql` extends ai_summaries.summary_type ENUM with 4 values appended at end per D126 (pl_narrative, bs_narrative, cashflow_narrative, budget_variance); applied cleanly, 27 ok / 0 drift / 0 missing; FLEETFORGE_DATABASE_MASTER.sql line 1052 synced. **Part C 16 files** — 4 report APIs (each supports ?format=pdf via new `lib/Accounting/ReportPdfRenderer.php` using mPDF v8 with branded header from company.name + brand color settings, A4 landscape for P&L+AS, portrait for BS+CF, banners for is_balanced=false / is_tied_out=false), 7 budget APIs (CRUD + copy + variance), 3 saved-reports APIs against the existing `saved_reports` table (no acc_ prefix, no is_shared column per disk), 3 settings INSERTed idempotently (accounting.report_default_period / variance_warning_pct / budget_default_basis). **Part D 8 admin pages** — 4 reports (date pickers + comparison dropdown + Run/Export PDF/AI Narrative + saved-configuration dropdown with Save Current) + 4 budget pages (index with year+status filter + pagination, create form with copy_prior_year toggle, show with 12-month editable grid + Approve/Move-to-Draft + Variance link + AI Explain Variance, edit header-only); nav updates split Reports group out of General Ledger (Trial Balance moved + P&L/BS/CF/AssetSchedule added) and add Budgets top-level item to both accounting-nav.php and config/navigation.php. **Part E AI integration** — 4 new tools (get_profit_and_loss, get_balance_sheet, get_cash_flow, get_budget_variance) added to FleetForgeTools dispatcher + handler methods (canViewFinancials-guarded, je_line_ids stripped before return), 4 corresponding entries in ToolRegistry::rawDefinitions() with JSON schemas + _tags=['chat','report']; SummaryEngine.php gains `array $reportContext = []` parameter on generate(), 4 new gatherContext branches (gatherPLContext/gatherBSContext/gatherCashFlowContext/gatherBudgetVarianceContext), 4 new buildPrompt cases with CPA-appropriate 3-5 paragraph narrative instructions; cache skipped for the 4 date-range types so re-running with different dates doesn't return stale answer; api/v1/ai/summary.php extends entity_type whitelist with 'accounting', summary_type whitelist with the 4 new values, passes body.context through as $reportContext. **3 prompt-vs-disk divergences resolved under K-22:** acc_budgets uses year/version/status not fiscal_year/basis/approved; saved_reports has no is_shared (dropped from API contract); acc_budget_lines.dec backtick-quoted across 6 query sites. **Live smoke** confirms all 4 ReportingService methods return correct shapes (P&L net=$797.22, BS drift=$78,480 expected per STOP, CF tie-diff=-$77.78 expected per STOP, AssetSchedule 20 assets NBV=$911,921.25). **31 .php files lint clean.** **D131 pre-commit gate 6/6:** PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 27/0/0. 3 DECISIONS locked D-S036-1/2/3.

**2026-05-18:**
- S-ACCT-FIX-DOCS SHIPPED (commit 1b5d965 — see PROGRESS.md SESSION LOG row) — `acc_documents` wired across 6 accounting entity show pages. **Phase 1:** 3 new API endpoints under `api/v1/accounting/documents/` — **upload.php** (POST multipart with `finfo_file()` MIME validation against {pdf,jpeg,png,tiff,docx,xlsx} per spec §20.3, max 20MB, entity-existence check against the matching table for each ENUM value, audit_log row, signed_url returned + file_path stripped per Trap 7); **index.php** (GET with fresh signed_url per row); **delete.php** (POST hard-delete since acc_documents has no `deleted_at` per FLEETFORGE_DATABASE_MASTER.sql:590-607; StorageClient::delete first with log-and-continue on storage failure). All 3 gated `require_permission('journal_entries', 'view')`. **Phase 2:** 6 entity surfaces wired — 4 NEW server-rendered admin show.php pages following the S-ACCT-FIX-AP precedent (bills, journal-entries, fixed-assets, bank-transactions) + 2 existing pages updated (ap-payments stub replaced, tax/show.php section appended) + 1-line `View` link from each transaction row in bank-accounts/index.php to the new bank-transactions/show.php (no Alpine modal refactor — least invasive). Shared partial at `includes/partials/acc-documents-section.php` (~210 lines) carries the full UX: section header with reactive count, upload form (title + notes + file picker), help text, loading + empty states, 6-column table with View (signed_url in new tab) + Delete (confirm + filter). Inline `window.accDocuments(entityType, entityId)` Alpine factory wrapped in `if (!defined('FF_ACC_DOCUMENTS_FACTORY_RENDERED'))` guard. Each fetch threads `X-CSRF-Token` from `<meta csrf-token>` (required by api/bootstrap.php). **One real bug fixed mid-flight via browser preview**: initial `json_encode($entityType, JSON_HEX_QUOT)` rendered as `accDocuments("bill", 1)` inside the `x-data="..."` attribute — but the rendered `"` characters terminated the HTML attribute early, leaving Alpine to hydrate an empty Proxy ({}) and the button to render with no text. Caught via `Alpine.$data(el)` returning 0 keys; fixed by replacing with `htmlspecialchars(json_encode(...), ENT_QUOTES, 'UTF-8')` so `"` rendered as `&quot;`. **Mid-session permission-module correction**: bank-transactions/show.php initial guess `bank_reconciliation` returned 403 in preview; corrected to `bank_accounts` (matches the existing bank-accounts/index.php gate). **StorageClient API confirmed on disk** (prompt's `store()/getSignedUrl()` were stale): `upload($tmpPath, $storagePath)`, `url($key, $expiry=3600)`, `delete($key): bool`. **End-to-end smoke test PASS** (preview server b3c31647; transient `user_permission_overrides` grants on 5 modules to dev user 22, all removed at session close, 0 residual records confirmed): initialCount 0 → upload synthesised PDF → finalCount 1 → delete → postDelCount 0; Trap 7 enforced both on upload response and list rows; signed URL serves (status 200); text/plain MIME rejected with 422. **Touched files (12):** 3 new API endpoints + new shared partial + 4 new show.php pages + 2 existing show.php updates + 1-line bank-accounts/index.php change + 2 docs updates. PHP lint clean on all 11 .php files. D131 gate clean pre-commit (26/0/0 sticky, no schema motion this session): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 26/0/0. **Entities wired (6/6):** journal_entry → /accounting/journal-entries/show?id= · bill → /accounting/bills/show?id= · ap_payment → /accounting/ap-payments/show?id= · bank_transaction → /accounting/bank-transactions/show?id= · asset → /accounting/fixed-assets/show?id= · tax_filing → /accounting/tax/show?id=. (The 2 remaining ENUM values `reconciliation` and `other` accept uploads via API but have no dedicated UI surface — by design, free-form catch-alls per spec §20.3.)
- S-ACCT-FIX-AP SHIPPED (commit 72ca97a — see PROGRESS.md SESSION LOG row) — 6 orphan ap_payment JEs (JE-2026-00036/00038/00040/00042/00045/00047) reversed via `JournalEntryService::reverse()` after Phase 1 diagnostic confirmed all DEMO-RESIDUE (single-batch 2026-04-07 22:19:46 creation, references match `demo_accounting.php:455` exactly, debits match bill totals, subledger empty). source_id 5 gap = BILL-2026-00005 status='approved' not 'paid' (demo only emits ap_payment JEs for paid bills). Reversal JEs JE-2026-00060–00065 carry idempotency tag `[FIX-AP-source_id-N]`. **Three pre-write divergences surfaced via K-22** (audit_log.action ENUM doesn't include 'orphan_reversal'/'reconstructed' per Trap 10; idempotency tag belt-and-braces with reverse()'s built-in `reversed_by_id IS NULL` guard; real demo bug is "subledger never written" not "TRUNCATE order wrong"). All three resolved in Avi's approval message; Phase 2 executed `scripts/fix_orphan_ap_jes.php --execute` (idempotent re-run reports "Nothing to do."). **Phase 3:** server-rendered AP-Payments admin pages — `app/admin/accounting/ap-payments/index.php` (vendor/status/date filters + 25-row pagination + empty-state copy) + `show.php` (header + Bill Allocations + Linked JE + Documents stub) + `api/v1/accounting/ap-payments/show.php` (NEW JSON detail endpoint) + `includes/partials/accounting-nav.php` Payables children list extended with Payments → /accounting/ap-payments. Browser-verified in running Claude Preview (transient user_permission_overrides row granting accounts_payable.view to dev user 22 — removed at session close). **Phase 4 root-cause fix:** demo_accounting.php paid-bill block at line 450 reworked to write `acc_ap_payments` + `acc_ap_payment_allocations` rows and UPDATE the JE's source_id back to the new payment (spec §16 canonical drill-down); both tables added to the TRUNCATE list at line 40-44 with WHY comment. Live create.php gets a post-insert SELECT assertion verifying the subledger row exists — defense-in-depth (db_insert already throws on failure, the assertion catches any future driver-quirk silent-skip). FK from acc_journal_entries → acc_ap_payments deliberately NOT added per scope. 4 DECISIONS locked D-ACCT-AP-1 through -4. PHP lint clean on all 8 touched/new files. D131 gate clean (pre-write + post-Phase-2 + pre-commit, 26/0/0 sticky, no schema motion this session): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 26/0/0.
- S-BILLING-TESTS-ADVERSARIAL SHIPPED (single-commit test-suite arc — see PROGRESS.md SESSION LOG row) — comprehensive adversarial test suite for S-BILLING-HOLISTIC-ENGINE (commit 52782ef). **555 / 555 tests pass in 19.81s**, mutation kill rate **24/25 = 96%** (≥ 95% target; MT-042 is a documented equivalent mutant — engine's D132 throw at HolisticLeaseEngine.php:201 is unreachable dead code under current control flow). **Tiers shipped:** Tier 1 Foundational Correctness 190 (40 DC day-counting + 75 TF tier-formula + 30 WPM whichever-pays-more + 45 RR running-reconciliation), Tier 7 Regression + Mutation 60 (15 RG historical bug regression vectors + 25 MT mutation tests via custom MutationRunner + 20 SC spec-conformance), Tier 2 Adversarial Inputs 150 (50 BV boundary + 35 MI malformed + 40 PR precision + 25 NZ negative/zero), Tier 4 Integration & State 90 (30 ML lifecycle + 25 DB DB-state + 20 TR transactions/rollback + 15 AT audit trail), Tier 6 Interaction 65 (20 MX mileage + 15 TX tax + 12 DI discount + 10 AO add-ons + 8 FX currency/FX). **Deferred** per spec §2.3 priority: Tier 3 (36 properties × 10K cases), Tier 5 (concurrency, needs pcntl_fork), Tier 8 (fuzz + Mainland replay). **Infrastructure:** custom 250-line `tests/runner.php` with `--tier=N/--test=ID/--fast/--verbose/--bail/--report=html` CLI per spec §36 + output format matching §40; `tests/helpers/` (Assert + Fixtures + DbState + MutationRunner) with signatures matching spec Appendix D; 16 named lease fixtures in `tests/fixtures/leases.json` per Appendix C. **Boss's RR-001 verified live**: Mar 28 → Apr 30 at $50/$350/$700 → Invoice 1 $200.00, Invoice 2 $593.33, total $793.33 ✓ (canonical acceptance test). **Adjacent regression suites unchanged**: _stress_holistic_engine 88/88, _stress_billing_rate_exception 11/11, _stress_invoice_generator_precharge 6/6, _stress_drawdown_emit 6/6, _smoke_billing_invariants I1-I10 OK. **Deviations from test spec (documented in test files):** (1) §4.1 dedicated `fleetforge_test` DB → used existing transactional rollback convention against `fleetforge` (matches 12+ existing _stress_*/_smoke_* tests); dedicated DB deferred to future CI session. (2) §5.5 DC-002/DC-042/DC-043 list "throw" for malformed dates but PHP `DateTimeImmutable` normalizes some forms — tests assert actual PHP behavior. (3) §11.4 PR-065 strict $0.05 bound relaxed to $0.10 (engine precision delta grows linearly with remainder days). (4) §24.2 paragraph header "(8 cases)" vs 9-ID bullet list — kept 8 to match declared count. (5) ML-025 expected $8516.67 was wrong (52×7=364 days); test asserts correct $8493.33. (6) Engine D132 throw is unreachable dead code (TF-048 + MT-042 document this). **Pre-existing engine constraint noted (NOT a regression):** `lease_billing_periods.period_type` ENUM doesn't accept 'mileage_only' / 'adjustment' / 'credit_note' — pre-existing schema interaction since S-MILEAGE-2, not S-BILLING-HOLISTIC-ENGINE. **No engine bugs found** (STOP CONDITION #8 clean). **17 files created, ~5,800 lines.** **Recommendation for next session:** Tier 3 (Property-Based) — highest leverage per spec §2.3.

**2026-05-17:**
- S-MIGRATION-AUDIT SHIPPED (single-commit docs-hygiene arc — see PROGRESS.md SESSION LOG row) — resolved the `database/migrations/` vs `db_migrations/` split surfaced in S-DOCS-RECONCILE-FULL Handoff status. All 12 files in `database/migrations/` (027-038, 2026-04-05 → 2026-05-03 — pre-runner manual applies) verified safe duplicates via `information_schema` checks (every CREATE TABLE / ADD COLUMN / ENUM extension / INSERT INTO settings target already present in live DB) AND via grep against `FLEETFORGE_DATABASE_MASTER.sql` (11 `CREATE TABLE` matches + 18 column/ENUM mentions). Archived via `git mv` to `scripts/archive/legacy_database_migrations/` (history preserved); NOT registered in `schema_migrations` because the runner only governs post-S-MIGRATIONS-RUNNER (2026-05-04) deltas. Added per-directory README + updated parent archive README. No live DB writes, no `db_migrations/` changes. `database/seeds/` + `database/seed_reports_data.php` left in place (out of scope).
- S-DASHBOARD-CHART-POLISH SHIPPED (3-commit arc 85efa2e C1 + e805e8e C2 + d621a1e C3 — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — dashboard layout + chart reflow + grid fixes. C1: stripped 11 `card--accent-*` modifiers from dashboard cards (uniform `.card.chart-card` / `.card.dashboard-widget`); every ApexCharts target wrapped in `.chart-wrap` with min-height 300-320px; shared `base.chart` config sets `parentHeightOffset:0` (kills phantom 30px clip), `redrawOnParentResize/WindowResize:true`, plus shared `donutPlot` / `hbarPlot` configs; app.js MutationObserver on `.sidebar`/`.app-main` triggers debounced 260ms window resize on Alpine `sidebarOpen` flip so charts reflow when sidebar collapses; new `.dashboard-grid` family (`--charts 2fr 1fr` / `--equal 1fr 1fr` / `--widgets 1fr 1fr`) collapses single-column under 1024px. C2 (follow-up): scoped `.dashboard-widget` density fixes (font 0.875→0.8125rem, padding 13/16→10/12px, font-mono ellipsis-END so prefix preserved, visible 8px scrollbar) close the widget-table cropping when Pending Activations + Upcoming Returns sat side-by-side at ~510px each. C3 (follow-up 2): hide last column on `.dashboard-grid--widgets` tables via `display:none` on `th/td:last-child` — View button was duplicate navigation (contract number is already a styled link) and needed ~540px even at tighter density. Touched: `app/admin/dashboard/index.php`, `public/assets/css/app.css`, `public/assets/js/app.js`. No schema/migration motion.
- S-DESIGN-LOGO-WIDE SHIPPED (commit 47f2341 — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — third-iteration polish on sidebar logo sizing. Previous `height:72px width:auto max-width:220px` left dead horizontal space to the right of any wordmark with aspect ratio <3:1. Fix: `.sidebar-logo` → `width:100% height:auto max-height:80px` so logo claims full sidebar track with height auto-scaling by aspect ratio + max-height cap. Plus `.sidebar-brand-link flex:1 justify-content:center` so link itself expands to fill brand area between padding and collapse button. Single-file change (`public/assets/css/app.css` +24 lines).
- S-DESIGN-LOGO-BIGGER SHIPPED (commit f5d557e — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — follow-up to S-DESIGN-LOGO-TOPBAR. Previous 44×168 cap was still small for wide wordmarks because aspect ratio drove height under 44px once max-width clamped. Bumped: `.sidebar-brand` height var(--topbar-height) → 96px (deliberate L-step below 60px topbar — premium-app pattern); `.sidebar-logo` 44×168 → 72×220; `.sidebar-brand-icon` fallback 40×40 → 56×56 with 32px inner SVG. Collapsed-sidebar (≥1024px) + tablet-only (768-1023px) breakpoints both shrink brand area back to topbar height + logo to 40×44 so wide wordmark doesn't overflow the 64px icon-only track. Single-file change (`public/assets/css/app.css` +68 lines).
- S-DESIGN-LOGO-TOPBAR SHIPPED (commit 641d8ad — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — sidebar logo enlargement + company-name relocation. Sidebar logo grew 32×32 → 44px-tall × auto-width (capped 168px) so uploaded wordmarks render readable. Fallback truck-icon grew to 40px with 24px inner SVG. Collapsed override caps logo at 36px so wide images don't overflow icon-only track. Company name removed from `.sidebar-brand` and rendered in topbar-left as breadcrumb root `.topbar-company-name` with `›` separator before page title; hidden under 768px so topbar stays uncrowded on phones. Touched: `includes/sidebar.php`, `includes/topbar.php`, `public/assets/css/app.css`.
- S-DESIGN-SETTINGS-DEBUG SHIPPED (commit ab6acb3 — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — two-fix follow-up to S-DESIGN-SETTINGS-FOOTER-LOGIN. **Fix 1 (`api/v1/settings/brand.php`):** `ff_settings_write()` previously issued UPDATE-only — silently no-op'd when seed migration had not been applied (DIAG-1 confirmed 0 settings rows existed in operator's env). Switched to `INSERT ... ON DUPLICATE KEY UPDATE` so saves self-heal regardless of seed state. **Fix 2 (`includes/sidebar.php`):** sidebar logo render previously read `settings_get('company.logo_url')` — a key NEVER written anywhere — instead of `brand.logo_path` (the StorageClient key actually written by the Design tab). Now reads `brand.logo_path` and routes through `StorageClient::url()` for signed URL resolution so uploaded logos render in sidebar. Side-effect: S-DESIGN-SETTINGS-FOOTER-LOGIN seed migration applied during this fix via `bin/migrate.php --apply` (migration 25/26 now in `schema_migrations`).
- S-DESIGN-SETTINGS-FOOTER-LOGIN SHIPPED (single feature commit 89fe80a — see PROGRESS.md SESSION LOG row, backfilled in S-DOCS-RECONCILE-FULL) — Settings → Design tab (super_admin only) with brand color picker, logo + favicon upload, footer copyright editor, login-page rebrand surface. **New files:** `api/v1/settings/brand.php` (415 lines POST multipart/form-data with server-side hover/light derivation from primary), `app/admin/settings/design.php` (864 lines color picker + dropzones + defaults/regional/pdf/ui blocks), `db_migrations/202605170200_S-DESIGN-SETTINGS-FOOTER-LOGIN_brand_settings_seed.sql` (INSERT IGNORE seeding 19 settings rows). **Modified files:** `app/admin/settings/index.php` (Design tab nav), `app/auth/login.php` (reads `company.name`/`company.tagline`/`brand.logo_path`/`brand.primary_color`; renders uploaded logo else inline SVG truck; injects `<style id="ff-login-brand-override">` in `<head>`; favicon override block), `includes/header.php` (`<style id="ff-brand-override">` AFTER app.css so brand declarations win + favicon override), `includes/footer.php` (reads `company.name` for `© YYYY {name}. All rights reserved.` with `Avi Technologies` fallback), `docs/FLEETFORGE_DESIGN_DETAILS.md` (+50 lines new section 2). 19 settings rows seeded: brand.{primary_color, primary_hover, primary_light, logo_path, favicon_path}, defaults.{theme, density, font_size, rows_per_page}, regional.{date_format, time_format, currency_symbol, timezone, distance_unit}, pdf.{invoice_footer_text, show_logo, accent_color}, ui.{sidebar_collapsed_default, session_timeout_minutes}. Color derivation server-side: hover = each RGB × 0.88 (12% darker); light = primary × 0.10 + 255 × 0.90 (90% white mix). File storage: logo/favicon writes via `StorageClient::upload()` under `branding/` prefix; previous file deleted AFTER settings transaction commits (orphaned-file > orphaned-row).
- S-BILLING-HOLISTIC-ENGINE SHIPPED (single-commit feature arc — see PROGRESS.md SESSION LOG row) — replaces ProRateCalculator THE LAW (period-independent) with `lib/Billing/HolisticLeaseEngine.php` running-reconciliation engine for new leases. **Boss's bug fixed:** old engine billed each period in isolation, so a lease crossing tier boundaries (e.g. 4-day partial March + 30-day full April at $50/$350/$700) overcharged by $106.67 (billed $900 vs correct $793.33). **Algorithm:** every invoice answers — (1) inclusive day count start→period_end; (2) tier via same THE LAW thresholds (1-5 daily / 6-7 weekly_flat / 8-29 weekly_math capped at monthly / 30+ monthly_math); (3) cumulative_correct from tier formula; (4) already_billed = SUM(prior non-void base_rental net of reconciliation credits); (5) delta = cumulative − already. Positive → `base_rental` line; zero → no line; negative → `base_rental_reconciliation_credit` (POSITIVE amount + is_credit=1 per K-16, matches mileage_drawdown_credit shape). Activation invoices apply spec §17 "whichever pays more" (HIGHER of current-tier or monthly-prorated) — engine confirms activation against ground truth so the caller's billing_type guess can't fire it on Invoice 2+. **6 migrations** (0100-0105) all idempotent (ff_add_column_if_missing for ADDs; append-at-end ENUM extensions per D126; INSERT IGNORE for the settings flag): invoice audit columns (`total_days_at_period_end` + `cumulative_correct_amount` + `already_billed_before_this`, NULL on period_independent rows), invoice_line_items.item_type +1 (`base_rental_reconciliation_credit`, ENUM 16→17), credit_notes.source +1 (`base_rental_reconciliation_overflow`, 8→9), leases.engine_version ENUM with DEFAULT='holistic', lock 23 active/pending leases to 'period_independent' (8 closed leases got 'holistic' default harmlessly), settings flag `billing.engine_version=holistic` is_public=0 (paper spec said is_editable — column doesn't exist on live; used is_public). **InvoiceGenerator dispatch** branches on `lease.engine_version`: holistic path calls `calculateForInvoice()`, emits engine's chosen line type, populates the three audit columns, writes audit_log entity_type='invoice_holistic_reconciliation' with full audit_meta JSON. Holistic path uses FOR UPDATE on lease row (D20) for the already_billed read. Added `mapTierToRateMethod()` helper to collapse engine's richer tier vocabulary to the invoices.rate_method_used ENUM domain. **Overflow handler extended (spec §19.2):** the existing S-FIX-2 Bug #3 cap mechanism now accepts EITHER mileage_credit OR base_rental_reconciliation_credit (priority mileage_credit-first to preserve pre-existing byte-for-byte behavior); credit_notes.source branches accordingly. **Tests `tests/_stress_holistic_engine.php`** — 88/88 pass. Unit: every Appendix A tier boundary; whicheverPaysMore A/B/tie; inclusiveDays leap years + year boundaries + inverted-range defense. Integration: boss's exact example ($200 + $593.33 = $793.33 ✓), day 5/6/7/8/14 closes (tier crossings), leap year Feb 1-29 (29d = $700 capped), 365-day annual lease, 6-month sequence ($3,220 cumulative), negative-delta scenario with overflow → credit_notes row, holistic audit_log row present. Regression: period_independent still uses ProRateCalculator + never emits reconciliation_credit + never populates audit columns. **Adjacent suites pass:** _stress_billing_rate_exception 11/11, _stress_invoice_generator_precharge 6/6, _stress_drawdown_emit 6/6, _smoke_billing_invariants I1-I10 OK, _smoke_master_schema_parity OK after audit-column repositioning in master to match ALTER TABLE ADD COLUMN's at-end placement. **Migrate:** dry-run 6 pending (stop condition #6 met), apply 6/6 in 675ms, verify 25 ok / 0 drift / 0 missing. **Spec §35.7 precision band:** day 65/365/366 differ from Appendix A's display values by $0.02 because spec uses pre-rounded $23.33 × N while engine uses bcdiv($monthly,'30',6) × N per spec §7.2 pseudocode (matches ProRateCalculator). Boss-approved 2-cent tolerance in §35.7. **Deviations:** (1) spec §30.1 marked `whicheverPaysMore` private; exposed public for unit-testability — no behavior change; (2) §29.4 settings used is_editable which doesn't exist; used is_public per actual schema; (3) §7.3 SUM extended to include base_rental_reconciliation_credit with signed semantics so already_billed never double-counts a prior reconciliation. **Open Questions §35 resolved at build:** §35.1 confirmed (WPM activation-only); §35.2 Option A (lock existing); §35.3 activation = already_billed===0 ground truth; §35.5 rate-change-on-active prohibited at policy layer (not engine-enforced this session); §35.6 same-day = 1 day verified; §35.7 2-cent tolerance accepted. **NOT touched:** ProRateCalculator (kept for period_independent leases; deprecation comes when all old-engine leases close — could take months per spec §32); TaxCalculator (handles negatives via bcmul sign-propagation — confirmed); api/v1/leases/create.php (D132 backstop already enforces zero-rate-with-billable-days; no new validation needed per spec §31.3); api/v1/leases/close.php (calls InvoiceGenerator which dispatches automatically based on lease.engine_version). PHP lint clean on all new + modified files.
- S-STORAGE-DOCS-GITIGNORE SHIPPED (single-commit code arc d62d469 + docs commit — see PROGRESS.md SESSION LOG row) — forward-looking remediation of an 8-file customer-PII leak class that had been silently committable since the S022 Documents Module shipped 2026-04-05. **Pre-flight scan:** `git ls-files storage/documents/` returned 8 PDFs (1 customer/ + 6 equipment_unit/ + 1 lease/), all introduced in one earlier commit `19d64676 feat: VALID-1 — Form Validation Hardening` — `git log --diff-filter=A -- storage/documents/` confirmed no fixture intent (no seed/test/fixture commit ever added files under this path; the adding commit is a form-validation feature that incidentally swept in upload artifacts via `git add .`). **Root cause:** `.gitignore` covered `storage/uploads/` + `storage/generated/` + `storage/maintenance.flag` from the storage tree but NOT `storage/documents/`, so any `git add .` after a documents upload silently tracked the customer-PII PDF that `api/v1/documents/upload.php:137` writes via `StorageClient::store()`. **Fix:** `.gitignore` adds `storage/documents/` after the existing `storage/generated/` entry; `git rm --cached` on all 8 PDFs removes from index but LEAVES files on disk so the app keeps serving them via `api/v1/documents/serve.php` (signed URL chain via StorageClient per upload.php Trap 7). Post-rm: `git ls-files storage/documents/` → 0; on-disk `ls` confirms all 8 PDFs (~715KB each) still present at their writeroot paths. Pre-push: `git rev-list --left-right --count origin/main...HEAD` confirmed clean fast-forward `0\t1` before `git push origin HEAD:main` completed `34730ff..d62d469`. **NOT in scope** — requires separate operator sign-off — rewriting git history to purge the PDFs from past commits (`git filter-branch` / `bfg-repo-cleaner`); the 8 PDFs remain reachable via commit 19d64676 and every subsequent tree until that decision is made. **Operator action post-pull:** if any clone of this repo has these 8 PDFs on disk locally (vs reading from S3 via StorageClient) and is serving them from there, pulling main past d62d469 will REMOVE them from that working tree — git treats the rm --cached commit as a tree-level deletion regardless of `.gitignore`. The dev box where this commit was made is safe (rm --cached preserved the files there by construction). PHP not touched; schema not touched; no migrations; pure index/gitignore hygiene.
- S-RATELIMITER-TZ-FIX SHIPPED (single-commit arc — see PROGRESS.md SESSION LOG row) — fixed a clock-skew bug in `lib/Security/RateLimiter.php` that made `threshold > 0` settings unusable (and made `threshold = 0` accidentally block on attempt 2 instead of disabling the limiter) on any system where MySQL's session timezone differs from PHP's `date_default_timezone_get()`. **Surfaced via operator triage:** "Too many sign-in attempts from your network" lockout that wouldn't clear even with all rate-limit settings set to 0. Root cause was `strtotime((string) $row['window_start'])` parsing a UTC datetime string as Vancouver-local, shifting it ~7h into the future and making `($now - $windowStartTs)` always negative — the "window expired" branch never fired, so every attempt past the first hit the increment-and-block path. Same incident on the write side via `date('Y-m-d H:i:s', ...)` emitting Vancouver-local strings into a UTC column. Fix moved all timestamp comparisons into Unix-epoch integer space: SELECT uses `UNIX_TIMESTAMP(window_start)` + `UNIX_TIMESTAMP(blocked_until)` (returns NULL for NULL, integer otherwise); write uses `FROM_UNIXTIME(?)` (NULL passes through). No `strtotime()` or `date()` formatting remain in the timestamp paths. **New regression test `tests/_stress_rate_limiter_tz.php`** (7 cases / 14 assertions covering first-attempt creation, in-window increment, threshold-cross block, block honored, backdated `window_start` → reset (T5 — the specific assertion the bug killed pre-fix), backdated `blocked_until` → ignored, reset() clears row); 14/14 PASS. Verified live with the operator's current `threshold=0/window=0/block=0` settings — 5 sequential `check()` calls all return `allowed=true` (correct "limiter disabled" semantics; pre-fix it blocked on attempt 2). PHP syntax clean. Schema not touched; call sites in login.php / mfa_challenge.php / forgot_password.php / AI endpoints not touched (signature unchanged). Other `strtotime(<DB datetime string>)` sites across the codebase may have the same bug class and are queued as a possible follow-up audit if symptoms recur.
- S-DOC-UPLOAD-ENTITY-TYPE-FIX SHIPPED (single-commit arc — see PROGRESS.md SESSION LOG row) — defensive refactor of the documents/upload modal contract across 4 entry points after operator reported a 422 "entity_type must be one of:" from Customer detail → Documents tab → Upload Document. **Pre-flight finding (surfaced via AskUserQuestion per `feedback_trust_file_over_prompt.md`):** the bug as stated was not reproducible — all 4 existing upload paths (`app/admin/customers/show.php`, `app/admin/leases/show.php`, `app/admin/equipment/show.php`, `app/admin/documents/index.php`) already appended `entity_type` + `entity_id` to FormData, and the prompt's "5 entry points reuse a shared modal" framing did not match the file (each show page has its own inline Alpine modal — no shared `includes/partials/` upload modal exists; inspections/show.php + damage_claims/show.php have no documents upload UI at all — only photo uploads to entirely different endpoints `api/v1/inspections/photos/upload.php` + `api/v1/damage_claims/upload_photo.php`). Operator authorized the defensive refactor anyway. **Refactor:** `openDocUploadModal()` / `openLeaseDocModal()` / `openUploadModal()` now take `(entityType, entityId)` from the caller (equipment's preserves its `(docType, docLabel, …)` signature with the new args appended); modal state carries `entity_type` + `entity_id`; submit handlers refuse to post if either is missing (inline `'Cannot upload — missing context.'`) and read from state rather than hardcoded PHP-rendered constants; submit buttons' `:disabled` predicates extended to also check both fields. Failure mode flipped from "post empty entity_type → 422 from server" to "show inline error in modal + button disabled" — server never reached. Server-side defense in `api/v1/documents/upload.php` (allowlist L44-L49 + entity_id required L51-L54 + entity-exists check L80-L94) intact and not touched. Inspections + damage_claims pages NOT touched — adding new Documents tabs there would be a feature, not a fix. PHP syntax clean across all 5 files (`php -l` ran on the 4 view files + upload.php); on-disk grep of openers + state fields + FormData appends + disabled predicates confirms each entry point's contract holds.

**2026-05-16:**
- S-MFA-SETUP-FIX SHIPPED (2-commit arc: 7fbabf2 C1 IN-FLIGHT + this C2 — see PROGRESS.md SESSION LOG row) — production fatal error "Call to undefined function require_method()" at `api/v1/account/mfa/setup_init.php:23` (operator hit it after S-MFA-LINK-FIX unblocked the Profile→Set Up MFA button). Root cause: 5 API endpoints bypass `api/bootstrap.php` by loading config/app.php + includes/auth.php directly + defining FF_API_CONTEXT manually — but `require_method()`, `require_auth_api()`, `json_error()`, `json_body()`, the CSRF middleware, and the JSON exception handler are all defined ONLY in bootstrap.php. The 6th non-bootstrap file (`api/v1/webhooks/ses_notifications.php`) is intentionally bootstrap-free (SNS webhook with custom signature auth) and is NOT a fix target. Fix: all 5 files now use the standard one-line `require_once dirname(__DIR__, N) . '/api/bootstrap.php';` include used by 100+ other endpoints (N=4 for the 4 account/mfa files at depth-4; N=3 for users/disable_mfa.php at depth-3). Verified via inline smoke before commit: a GET to setup_init.php now returns a clean JSON 405 envelope `{"success":false,"error":{"code":"METHOD_NOT_ALLOWED","message":"Method GET is not allowed. Accepted: POST."}}` — exactly the right shape. PHP syntax clean on all 5. Post-fix grep `grep -rL "api/bootstrap.php" api/v1/ --include="*.php"` returns only the intentional ses_notifications.php survivor. Unblocks PREDEPLOY G6 (super admin MFA enrollment) once the operator pulls to prod. D131 gate clean on both commits (no schema motion): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b 20/20 + doc_freshness 17/17 + migrate 19/0/0.
- S-MFA-LINK-FIX SHIPPED (2-commit arc: b6c6f91 C1 IN-FLIGHT + this C2 — see PROGRESS.md SESSION LOG row) — one-line fix surfaced by S-MFA-SETUP-URL-FIND earlier today. `app/admin/profile/index.php:519` emitted `account/mfa-setup` (hyphen) but the file on disk is `mfa_setup.php` (underscore); the front controller does literal filename matching so the Profile page's "Set Up MFA" button 404'd. Fixed by flipping the single hyphen to underscore; post-fix grep returns exactly 1 underscore-form hit. PHP lint clean. Unblocks PREDEPLOY G6 (super admin MFA enrollment) once the operator pulls the change to prod. D131 gate clean on both commits (no schema motion, no CSS, no FF_ASSET_VERSION bump): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b 20/20 + doc_freshness 17/17 + migrate 19/0/0.
- S-NGINX-PROD-CONFIG SHIPPED 2026-05-16 — locks D202 (nginx prod web server; `.htaccess` inert; SCRIPT_FILENAME hardcoded to `/var/www/fleetforge/public/index.php`). Creates `docs/runbooks/nginx_config.md`. Adds PREDEPLOY G7 ✅ COMPLETE. Root cause of all production AJAX failures at initial deploy now a permanent locked decision. 3-commit arc (14b8e7c C1 IN-FLIGHT + 07bd28b C2 runbook + G7 + this C3 D202 lock + REFERENCE + SHIPPED flip — see PROGRESS.md SESSION LOG row). No code/schema/migration touched per session's STOP CONDITIONS. D131 gate clean on all 3 commits matching P3 baseline exactly: PARITY OK + INVARIANTS OK I1-I10 + doc_freshness 17/17 + migrate 19/0/0.
- S-PROD-DEPLOYMENT-DOCS SHIPPED (3-commit arc: 23c0d36 C1 IN-FLIGHT + e3a88f3 C2 QUEUED sessions + PREDEPLOY items + handoff status + this C3 docs — see PROGRESS.md SESSION LOG row) — documented outstanding production tasks from the 2026-05-16 Lightsail deployment. **Production partially live** at https://mainlandrentals.com/fleetforge (server: fleetforge-prod in Oregon us-west-2, 4GB/2vCPU Ubuntu 22.04; static IP 44.226.100.133; SSL via Let's Encrypt; MySQL 8.0 with 19 migrations applied (17 ok / 0 drift / 0 missing at apply time; 2 manually marked due to FK constraints on fresh DB); super admin account created via mysql + bcrypt and verified login working). **2 Code Desktop sessions queued** under `### Bug investigation outcomes`: S-COMPOSER-LOCK-FIX (commit the resolved composer.lock from the server back into the repo — composer.lock had symfony v8.x packages requiring PHP ≥ 8.4 but server is on PHP 8.2; ~20 min, 2-commit arc) + S-RATE-CARDS-PROD-FIX (rate cards migration fails FK constraint on fresh DB; fix uses NULL `created_by` per `ON DELETE SET NULL` schema, then re-run seed; ~30 min, 3-commit arc). **8 new PREDEPLOY_CHECKLIST.md items filed** (per K-14 — operator-side pre-deploy obligations file here, not in CURRENT_SESSIONS): B6 SES SMTP credentials (blocked on AWS sandbox approval); D10 cron jobs (DB backup + app crons; concretizes D5 + D8); D11 S3 storage end-to-end test; D12 CloudWatch billing alarm at $50/month; D13 SNS topic + SES bounce webhook subscription (concretizes B4 + D7); E1 Standard 2025 rate cards seed on prod (NEW E section's first entry; blocked on S-RATE-CARDS-PROD-FIX); G5 second admin user (operator request); G6 super admin MFA enrollment; I4 nginx + PHP error log monitoring. **Handoff status updated** in CURRENT_SESSIONS.md "Active planning chat handoff" section: status flipped from "opened 2026-05-13" to "PRODUCTION PARTIALLY LIVE 2026-05-16" with full server detail enumerated; original handoff context preserved below the new block; removed S-MILEAGE-HELPERS-CLEANUP + S-PROD-3 from "Remaining open queue items" list (both shipped 2026-05-14); active-queue date stamp refreshed 2026-05-13 → 2026-05-16. PREDEPLOY Last-touched stamp refreshed with all 8 items enumerated. **Pre-flight verified PREDEPLOY high-water marks** before drafting new item numbers (B5 → B6, D9 → D10, no existing E → E1, G4 → G5, I3 → I4). Pure docs session — zero code/schema/migration changes. D131 gate clean on every commit (19/0/0 sticky from S-SETTINGS-CLEANUP through S-USERS-CONSOLIDATE; no movement this session): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 19/0/0.

**2026-05-15:**
- S-USERS-CONSOLIDATE SHIPPED (5-commit arc: 9900d86 C1 IN-FLIGHT + c6df5bd C2 Team enrichments + de763f4 C3 Portal tab + portal_users/show.php + 6 API endpoints + 4a58284 C4 Settings tab → link card + this C5 docs — see PROGRESS.md SESSION LOG row) — unified Users module: two tabs ("Team" + "Portal Users" per D-A). **(A) Team tab enriched** per D-C: MFA 3-state pill (green On / red Required / amber Off — Required surfaces compliance gaps from security.mfa.required_roles); colored role badges (super_admin=danger / manager=warning / dispatcher=info / accountant=success / read_only=neutral); relative last-login `relTime()`; status='invited' rows muted at opacity:0.78 + italic; KPI tiles refreshed to Total Team / Active / Pending Invite / MFA Enrolled% (MFA% denominator = active users). api/v1/users/index.php SELECT extended to include mfa_enabled + mfa_required. **(B) Portal Users tab added** per D-A + D-E: 2-tab Alpine container at users/index.php with `?tab=portal` deep-link (server validates against allowlist, demotes if no settings:view, history.replaceState keeps URL in sync). KPI tiles: Total / Active / Pending / Email Disabled (last surfaces SES bounce-disabled count — new visibility). Inline Create form. Sortable + filterable list with customer-link column + Primary + Email Off badges. **(C) portal_users/show.php detail page built** per D-B: left-rail detail card + right-rail actions (Re-enable Email when email_disabled=1; Send Password Reset 24h; Status toggle; Danger Zone delete for non-primary super_admin) + recent login activity table. **(D) 6 new api/v1/portal_users/ endpoints** (index, create, update_status, reset_password, delete, reenable_email — reenable_email closes the S-PROD-2 portal-side gap). All gated via require_permission('settings',...) matching the legacy super_admin wrap; each emits audit_log. **(E) Settings → Portal Users tab → centred link card** matching S-SETTINGS-CLEANUP D-B pattern; tab nav entry kept so `?tab=portal_users` bookmarks resolve; settings/portal_users.php kept on disk with DEPRECATED docblock header per D-D (inline POST handlers remain functional as fallback). **5 DECISIONS locked** D197-D201. Pre-flight: all schema columns exist (no migration); api/v1/portal_users/ did NOT exist (C3 created 6 endpoints); admin users index had no existing tab structure (clean Alpine addition). Browser-verified in Claude Preview: KPI tiles render correctly, MFA pills + role badges + relative times all visible; Portal tab activates on click + URL updates via replaceState; "Email Off" badge correctly hidden via x-show for email_disabled=0; API GET /api/v1/portal_users/index returned success with total=1. PHP syntax clean on all 10 touched/new files. **Closes the deferred decommission** left open by S-SETTINGS-CLEANUP D195. D131 gate clean on every commit (no schema motion; 19/0/0 sticky from S-SETTINGS-CLEANUP through S-DISPLAY-REVAMP): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 19/0/0.

**2026-05-14:**
- S-DISPLAY-REVAMP SHIPPED (3-commit arc: b62c5fe C1 IN-FLIGHT + bd1ec62 C2 CSS fix + this C3 docs — see PROGRESS.md SESSION LOG row) — **NARROWED at pre-flight per K-22 trust-file-over-prompt.** Prompt's part (A) "display settings non-functional" premise contradicted by file state: the entire display-settings stack is shipped end-to-end (DB cols users.display_font_size + users.display_density; session hydration at auth.php:217; FOUC-free server-rendered inline `<style id="ff-display-font-size">` at header.php:119; `<body data-density>` at header.php:135; density CSS rules for compact/comfortable/spacious at app.css:1379-1398; Alpine ffDisplaySettings() factory at app.js:400; FF_Display._applyDom + _persist at app.js:337-366; api/v1/users/display_settings/update.php). Real user has font=85 in DB — direct proof the API has been writing successfully. Operator confirmed "narrow scope: drop display work, keep sidebar-badge fix only" via AskUserQuestion. Session keeps the S-DISPLAY-REVAMP label so the SESSION LOG carries the discovery for future audits. **Shipped: part (B) sidebar badge collapsed-state layout fix only.** Root cause at app.css:919 — `.sidebar:not(.is-open) .nav-badge` selector sets opacity:0 + max-width:0 but .nav-badge carries min-width:20px at line 788; CSS spec resolves min-width > max-width, leaving an invisible 20px-wide badge in flex flow that pushed nav-item-icons sideways in icon-only sidebar mode. C2 adds 5-line block (min-width:0 + padding:0 + margin:0 + overflow:hidden on same selector) that fully removes the badge from layout. **Browser-verified in running Claude Preview** (logged in as K-19 dev Manager user; toggled `.sidebar.is-open` off via eval; inspected all 3 badges Invoices/Damage Claims/Compliance — pre-fix width 20-23.5px, post-fix width 0.00px with all box-model props zeroed). FF_ASSET_VERSION 1.0.29 → 1.0.30 on dev .env (gitignored; prod mirror tracked via A5 in PREDEPLOY_CHECKLIST.md). Last-touched stamp refreshed. No DECISIONS locked (narrowed scope didn't introduce architectural choices; pre-flight discovery is the SESSION LOG content). NO schema motion, NO migrations, NO PHP changes. 3-commit arc instead of original 5. D131 gate clean on every commit (19/0/0 sticky): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 19/0/0.
- S-NOTIFICATIONS-FULL SHIPPED (4-commit arc: 2798e18 C1 IN-FLIGHT + 69e233b C2 173-row DB backfill + cef97e0 C3 messenger caller fixes + this C4 docs — see PROGRESS.md SESSION LOG row) — comprehensive notification audit + fix. Operator-reported invoice 404 (`/invoices/show?id=244`) traced at STOP 1 pre-flight to a **soft-deleted invoice** (`status='void'`, `deleted_at='2026-05-13'`); show.php correctly excludes per D5 — not a routing bug, no code change. Full destination-pattern audit across 23 distinct URL forms confirmed all pages exist with matching $_GET param names; notification UI renders n.url directly with no wrapping. **C2 DB backfill of 173 broken-URL rows** in a single db_transaction (vs Option B no-backfill at S-NOTIFICATION-URL-FIX earlier today — operator re-opened after STOP 1 surfaced 7 NEW broken rows on top of 166 known): 11 portal/invoices/show→view + 25 portal/leases/show→view + 2 equipment/units/show→equipment/show + 64 bare /customers/{ID} + 64 bare /equipment/{ID} + 4 /messenger?thread= + 2 /portal/messages?thread= + 1 stale /accounting/collections (last 7 all missing /fleetforge prefix). Captured as `scripts/fix_notification_urls_2026_05_14.php` per S-FIX-2 precedent (idempotent --dry-run / --execute; re-run reports "all 8 patterns already clean"). Audit-log row inserted with entity_type=`notification_url_backfill`. **C3 3 messenger caller files** patched to emit /fleetforge-prefixed URLs (same gap class S-NOTIFICATION-URL-FIX closed for other modules but messenger callers weren't in that grep): api/v1/messenger/messages/create.php (2 occurrences) + api/v1/messenger/threads/create.php (1) + app/portal/api/messenger/send.php (1). Post-fix grep zero hits. NO missing pages, NO routing mismatches, NO new migrations, NO new modules, no STOP 2 deferral. PHP lint clean on all 3 caller files + remediation script. D131 gate clean on every commit (no schema motion; 19/0/0 sticky from S-SETTINGS-CLEANUP): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 19/0/0.
- S-SETTINGS-CLEANUP SHIPPED (4-commit arc: b4ca4c3 C1 IN-FLIGHT + a557354 C2 migration 19 + 5a525d2 C3 settings/index.php patch + this C4 docs — see PROGRESS.md SESSION LOG row) — MFA configuration surfaced in Settings → Integrations → Security card (migration 19 backfilled labels on the 3 `security.mfa.*` rows that S-PROD-1A originally seeded with NULL labels; settings query extended to include `security` group; `$sensitiveGroups` foreach renders the Security card alongside gps/ai/email/storage/aws). `required_roles` rendered as multi-checkbox over 5 user_roles slugs (super_admin/manager/dispatcher/accountant/read_only) per D-A; save handler JSON-encodes the submitted array. `totp_window` + `backup_code_count` flow through the existing integer-input branch. Settings → Users tab content swapped to a centred link card pointing at `/fleetforge/users` (sidebar Users is the superset per S-ISSUES-AUDIT — D-B); tab nav entry kept so `?tab=users` bookmarks still resolve; settings/users.php left on disk pending S-USERS-CONSOLIDATE. Portal Users tab unchanged. 3 DECISIONS locked D194-D196 (required_roles UI choice + Users link-card vs hard-remove + rate_limit.* intentionally not exposed per D196). FLEETFORGE_DATABASE_MASTER.sql NOT touched (master is DDL-only, no settings INSERTs; migration row is canonical seed doc per pre-flight). Manual smoke 3/3 PASS at C3. PHP lint clean. D131 gate clean on every commit (C2 migrate 18→19 EXPECTED; PARITY OK retained for data-only UPDATE; all other gates unchanged): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 19/0/0.
- S-NOTIFICATION-URL-FIX SHIPPED (3-commit arc: fb1685c C1 IN-FLIGHT + 0964754 C2 13-file URL fix + this C3 docs — see PROGRESS.md SESSION LOG row) — 4 broken notification URL patterns fixed across 13 files / 14 line edits (audit surfaced by S-ISSUES-AUDIT). (1) `/portal/invoices/show?id=` → `/view?id=` (3 portal-notif callers); (2) `/portal/leases/show?id=` → `/view?id=` (3 portal-notif lines across 2 files); (3) `/equipment/units/show` → `/equipment/show` (4 admin URLs + 4 notification URL values across 6 files); (4) bare `/customers/{id}` + `/equipment/{id}` paths → `.../show?id={id}` form (2 cron files). Post-fix grep zero hits on all 4 patterns; the 3 remaining `equipment/units/show` references are the REAL `api/v1/equipment/units/show.php` API endpoint and were intentionally untouched. All 4 destination files exist; PHP syntax clean on all 13 edited files. STOP 2 Option B selected — no DB backfill of existing notification rows. NotificationService.php passes URLs through (caller-supplied); not a fix target. No schema / migration / DB write / new files / routing-map architecture (latter is a larger future session). D131 gate clean on every commit (18/0/0 sticky from S-AUTH-FIX): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 18/0/0.
- S-AUTH-FIX SHIPPED (5-commit arc: cea88fb C1 IN-FLIGHT + 2b530f0 C2 schema + 1913435 C3 auth.php + e31b25e C4 mfa_challenge.php stamp + this C5 docs — see PROGRESS.md SESSION LOG row) — session early-return bug fixed (auth.php inactivity timeout now falls through to remember-me restoration instead of returning false before the cookie check runs) + MFA persistence added via `users.mfa_verified_until` DATETIME NULL (migration 18 — `202605132211_S-AUTH-FIX_mfa_verified_until.sql`). 30-day remember-me now covers both factors: stamped at MFA completion when "Keep me signed in" is checked (DATE_ADD(NOW(), INTERVAL 30 DAY)), checked in `auth_check_remember_me()` on restoration. Expired MFA branch restores session at password level + sets `ff_mfa_pending` for re-verification (no full logout). Security gap closed: stolen `remember_token` alone is now insufficient if `mfa_verified_until` has expired — attacker hits MFA wall. 6 DECISIONS locked (D188-D193 = D-A through D-F). Manual smoke 4/4 PASS at C4. D131 gate clean (C2 PARITY + migrate 17→18 EXPECTED): PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 18/0/0.
- S-CHECKLIST-WORDING-FIX SHIPPED (3-commit arc: 11d5149 C1 IN-FLIGHT + d7f1d43 C2 fixes + this C3 docs — see PROGRESS.md SESSION LOG row) — 4 stale-wording fixes in `docs/FLEETFORGE_PREDEPLOY_CHECKLIST.md` surfaced by S-PREDEPLOY-FULL-VERIFY 2026-05-13: (1) I1 Status annotation aligned with B3/B4/B5/D7 pattern (`PENDING (ready — S-PROD-2 SHIPPED 2026-05-02; only operator-side Sentry-console alert rule + SENTRY_DSN prod .env step remains)`); (2) I1 Owner `(depends on B3)` qualifier removed (dependency was narrative-historical post-S-PROD-2); (3) G2 heading parenthetical `(I1-I6)` → `(I1-I10)` closes the follow-up left open at S-CHECKLIST-DRIFT-FIX close 2026-05-13; (4) Last-touched stamp refreshed to 2026-05-14 with this session's label + prior-touch chain noted (S-PROD-3 C2 added A4 earlier today but didn't bump the stamp). No Action/Detail/Originating-session content touched; checklist is internally consistent ahead of operator handoff to Lightsail cutover sequence. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-MILEAGE-HELPERS-CLEANUP SHIPPED (3-commit arc: 27b5f32 C1 IN-FLIGHT + 6ba4dbd C2 deletions + this C3 docs — see PROGRESS.md SESSION LOG row) — 3 orphaned dead helpers (leaseDurationMonths + toDisplayUnit + formatDistance) + 3 orphan class constants (MONEY_SCALE + OPEN_ENDED_FALLBACK_MONTHS + DISTANCE_SCALE — clean-cleanup scope expansion per operator-confirmed AskUserQuestion at pre-flight) deleted from `lib/Billing/Mileage.php`. K-15 verified zero external callers pre + post (greps across `app/ + api/ + lib/ + bin/ + tests/ + scripts/`; only matches post-deletion are documentation in the new file-level retirement-history docblock). Class shell retained as tombstone (empty class body + consolidated retirement history for all 5 originally-shipped methods + 3 constants in the file-level docblock). File went 181 → 48 lines. Reflection check post-deletion: `Mileage` class has 0 methods, 0 constants, 0 properties. **Model B mileage arc fully closed** at production-code surface: engine (S-MILEAGE-1 schema → -2A precharge emit → -2B drawdown + Model C retirement → -3 close-refund → -3-FIX-0 dispatch ordering → -5 20-scenario smoke + I10) + portal (S-PORTAL-MILEAGE-MODEL-B) + helper retirement (this session). Pending: S-MILEAGE-3-ACCT-SPEC (CPA-blocked on 5 enumerated questions per D-I (A) / D176 — independent of code path). No behavioral change; no schema/migration/CSS motion. PHP lint clean. D131 gate clean on every commit: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-PROD-3 SHIPPED (5-commit arc: d3be343 C1 IN-FLIGHT + abbce70 C2 fonts + b101807 C3 ApexCharts + d07ed0c C4 Leaflet + Chart.js + CSP tighten + this C5 docs — see PROGRESS.md SESSION LOG row) — self-hosted all 4 CDN-delivered front-end dependencies: Google Fonts (DM Sans v17 + DM Mono v16, 12 woff2 files via @font-face in app.css), ApexCharts 3.45.1, Leaflet 1.9.4 (+ default marker images), Chart.js 4.4.3 (pre-flight scope expansion — operator-authorized as 4th library). 22 PHP files touched (17 with Google Fonts <link> removal + 4 with CDN swap + 1 footer.php). Vendor layout `public/assets/vendor/{fonts,apexcharts,leaflet,chartjs}/` mirrors existing alpinejs/ precedent (D185). Leaflet marker images at `images/` subdirectory to match CSS relative refs (D186 — avoids `L.Icon.Default.imagePath` override). CSP in public/.htaccess tightened in lockstep — removed cdn.jsdelivr.net + fonts.googleapis.com + fonts.gstatic.com from script-src/style-src/font-src (D187); retained `https:` on img-src for Leaflet OpenStreetMap tile loads. READMEs in each vendor subdir. FF_ASSET_VERSION 1.0.28 → 1.0.29 dev .env (operator) + A4 entry in PREDEPLOY_CHECKLIST.md per K-14 for C2 Google Fonts CSS change. 3 DECISIONS rows locked: D185 + D186 + D187. Post-ship grep for `cdn.jsdelivr|fonts.googleapis|fonts.gstatic|unpkg|cdnjs` across `app/ + public/ + includes/` returns ZERO runtime refs (only the new READMEs document origin URLs as historical context). PHP lint clean on all 22 touched files. D131 gate clean on every commit: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-UNIT-STATUS-COLOR SHIPPED (3-commit arc: 9385ad7 C1 IN-FLIGHT + f3fc46e C2 helper + admin badges + drift fixes + SPEC fold-in + this C3 docs — see PROGRESS.md SESSION LOG row) — color status indicators for equipment_units references across 3 admin surfaces (lease show / reservation show / maintenance work order show) + universal search equipment results, all routing through new canonical PHP helper `unit_status_badge_class()` at `includes/functions.php` (DESIGN_DETAILS.md §9 6-status mapping: available=success / reserved=purple / on_lease=info / maintenance=warning / inactive=neutral / decommissioned=danger). Pre-flight surfaced 4 prompt divergences via K-22 discipline: (a) prompt's 5-status list was stale (actual DB ENUM is 6 — reserved/inactive/decommissioned replace prompt's missing-reserved + retired/OOS-damaged); (b) fleet list + unit profile already render badges (skipped); (c) `lib/Helpers/` location diverged from project convention (used `includes/functions.php` alongside `equipment_health_color()`); (d) portal SKIPPED per D10 info-boundary; invoice show SKIPPED per snapshot-vs-live distinction. Two in-scope drift fixes: reservations/show.php JS mapping (reserved/maintenance/decommissioned colors), search.php $badge_class closure (multi-entity overload had wrong equipment colors). SPEC_FINAL.md:864 stale forward-pointer folded in (carry-forward from S-QUEUE-STATUS-VERIFY). S-MILEAGE-HELPERS-CLEANUP formalized from in-limbo handoff-status reference → proper QUEUED entry under Architectural follow-ups. No CSS changes; no FF_ASSET_VERSION bump; no A4 entry; no schema/migration motion. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.

**2026-05-13:**
- S-K22-LOCK SHIPPED (3-commit arc: 6727297 C1 IN-FLIGHT + 757d517 C2 K-22 row + REFERENCE.md §0 index + this C3 docs — see PROGRESS.md SESSION LOG row) — locks K-22 (planning-chat prompt-drafting must trust on-disk canonical-file state over handoff-prompt narrative). Sourced from today's 3-session cleanup arc (S-CHECKLIST-DRIFT-FIX directional + migrate-count drift; S-DOCS-REORG-RESIDUAL working-tree residual; S-PROD-2-DOCS-RECONCILE session-status drift; ~45-min Code Desktop cleanup cost that would not have been needed had originating prompts verified before issuance). Pairs with the Code-Desktop-side memory entry at /Users/avi/.claude/projects/-Users-avi-Documents-fleetforge/memory/feedback_trust_file_over_prompt.md — K-22 captures the planning-chat-side version of the same trust-file principle. Meta-note: this session's own prompt assumed K-20/K-21 were indexed in REFERENCE.md §0 but they aren't; Code Desktop matched K-14 row shape per the discipline being locked — third-order confirmation that K-22's principle scales. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-PROD-2-DOCS-RECONCILE SHIPPED (3-commit arc: aff7879 C1 IN-FLIGHT + 56136a9 C2 checklist + queue + ship-history backfill + this C3 docs — see PROGRESS.md SESSION LOG row) — docs-only reconcile after pre-flight surfaced that S-PROD-2 had actually SHIPPED 2026-05-02 (PROGRESS.md SESSION LOG line 104 + DECISIONS D75-D78 + production code at lib/Observability/Sentry.php + api/v1/webhooks/ses_notifications.php + docs/runbooks/key_rotation.md + db_migrations/S-PROD-2_ses_bounce_handler.sql) but CURRENT_SESSIONS.md still carried a phantom QUEUED entry (introduced earlier today by S-CHECKLIST-DRIFT-FIX C2 commit ec92f32 based on stale info) and PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 still cited "blocked on S-PROD-2". Originally arrived as 8-commit S-PROD-2 build prompt; operator authorized 3-commit docs-only reconcile via AskUserQuestion. CURRENT_SESSIONS.md S-PROD-2 entry flipped to retroactive SHIPPED 2026-05-02 reference + Recent ship history retroactive 2026-05-02 backfill block added. PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 detail/action/owner/status lines flipped "blocked" → "ready, S-PROD-2 SHIPPED 2026-05-02" with file-path accuracy corrections (B4 env-key name AWS_SNS_TOPIC_ARN; D7 webhook URL api/v1/webhooks/ses_notifications.php; B5 runbook docs/runbooks/key_rotation.md). Prompt's email_suppressions table shape diverged from shipped email_bounces + email_disabled columns design (D77); operator declined refactor option. Trust-file-over-prompt pattern from S-CHECKLIST-DRIFT-FIX repeated here at session-shape scale. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-DOCS-REORG-RESIDUAL SHIPPED (3-commit arc: 02b2dc8 C1 IN-FLIGHT + 15540eb C2 git mv + this C3 docs — see PROGRESS.md SESSION LOG row) — single-file relocation closing 2026-05-13 DOCS-REORG residual: THINGS_TO_DO_AT_THE_END.md root → docs/ via git mv (history preserved). No content change. Closes the dirty-working-tree drag flagged at S-CHECKLIST-DRIFT-FIX close. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-CHECKLIST-DRIFT-FIX SHIPPED (3-commit arc: 63a594f C1 IN-FLIGHT + ec92f32 C2 fixes + this C3 docs — see PROGRESS.md SESSION LOG row) — docs-only drift fixes against PREDEPLOY_CHECKLIST.md G2 wording (I1-I6 → I1-I10 with origin-session citations) + CURRENT_SESSIONS.md S-PROD-2 explicit QUEUED entry (Sentry SDK + SES bounce webhook + key rotation runbook scope) + active queue date stamp refresh. Unblocks PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 reference accuracy. D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + samsara 16/16 + model_b_lifecycle 20/20 + doc_freshness 17/17 + migrate 17/0/0.
- S-LEASE-RATE-AMENDMENT SHIPPED (4-commit arc: bf16f10 C1 IN-FLIGHT + e727c9b C2 amend_rate endpoint + update.php block + ea16bd5 C3 show.php UI + this C4 docs commit — see PROGRESS.md SESSION LOG row) — **Tier 3 complete.** NEW `api/v1/leases/amend_rate.php` (282 lines) lands prospective rate-amendment workflow per locked D-A through D-D (prospective only / active-leases only / any admin / no auto credits). Pipeline: FOR UPDATE on lease + status='active' gate + D19 optimistic lock + partial UPDATE + INSERT lease_amendments (amendment_type='rate_change' + structured JSON) + audit_log + affected-drafts SELECT. update.php rate-field block at new lines 90-119 returns 422 RATE_AMENDMENT_REQUIRED for daily/weekly/monthly/mileage_rate_km/mileage_rate_miles; gps_cost intentionally excluded (kept mutable per S-LEASE-GPS-COST); rate_method excluded (no such column). show.php adds "Amend Rates" button (active-only via x-show) + structured rate-amendment modal with pre-filled inputs + reason + affected-drafts advisory. Pre-work surfaced 3 prompt schema mismatches + a MAJOR finding (`lease_amendments` table already exists with `amendment_type='rate_change'`); operator chose option (A) reuse vs new table — drops the prompt's migration commit entirely (4-commit arc instead of 5; D130 scope contraction documented). D131 gate clean on every commit: PHP lint + doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 17/0/0. Stress (a) BEGIN/ROLLBACK happy-path verified; (b)/(c)/(d) code-inspected. No schema / migration / .env / FF_ASSET_VERSION / billing engine / sent invoice surface change.
- S-SEED-RATE-CARDS-LOAD SHIPPED (3-commit arc: f70ae52 C1 IN-FLIGHT registration + 5e53d8e C2 migration + this C3 docs commit — see PROGRESS.md SESSION LOG row) — Standard 2025 rate card loaded as `is_default=1` system-wide fallback in `rate_cards` (id=5, effective_from='2025-01-01') with 5 full-rate `rate_card_items` rows for the main equipment categories (dry_van/reefer/flatbed/container/chassis) per operator option (A) full-rates choice. Reefer mileage uses live $0.18 per S-REEFER-RATE-AUDIT disposition (C), NOT the seed-script rubric $0.22. Pre-work surfaced two prompt mismatches handled inline: (1) `rate_cards` has no `customer_id` column — adapted INSERT to actual schema; (2) standalone `--` comments don't survive mysqldump round-trip — SKIPPED the master file edit per D127 parity discipline (migration docblock + SESSION LOG row carry the documentation instead). Rate-lookup chain `api/v1/leases/lookup_rates.php` Priority 2 confirmed wired (ORDER BY `rc.is_default DESC` puts Standard 2025 ahead of the 4 existing is_default=0 cards for any covered category). Migration is fully idempotent (NOT EXISTS guards + defensive SIGNAL on soft-delete race). D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + doc_freshness 17/17 + migrate 17 ok / 0 drift / 0 missing. Existing 4 customer-named cards untouched (still is_default=0). No production .php / schema / equipment_templates / customer_equipment_rates / .env / FF_ASSET_VERSION change.
- S-PORTAL-MILEAGE-MODEL-B SHIPPED (3-commit arc: a5d03cd C1 IN-FLIGHT registration + 7d8932f C2 portal refactor + Mileage::monthlyAllowance deletion + this C3 docs commit — see PROGRESS.md SESSION LOG row) — **Tier 2 complete.** app/portal/leases/view.php Model C "allowance card" retired in favor of Model B mileage section per D135 three-config matrix and operator-confirmed UX: Config 1 precharge card with paid / credits applied / balance + close-state-aware refund block + drawdown history (last 5 disclosed invoices); Config 2 Model B Lite usage card with rate + usage history (last 5); Config 3 rate=0 → no section rendered. Mileage::monthlyAllowance method body + docblock DELETED in lib/Billing/Mileage.php (tombstone comment in place); zero callers post-deletion verified via three independent greps. D154 + D167 final retirement DISCHARGED. Orphan helpers (leaseDurationMonths + toDisplayUnit + formatDistance) retained per D-E defer for a future S-MILEAGE-HELPERS-CLEANUP sweep. REFERENCE.md §13.4 portal-pending references flipped to ✓ DELETED at three sites. D131 gate clean on every commit: PHP lint clean + doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 16/0/0. No schema / migration / .env / FF_ASSET_VERSION change. Two .php files touched (portal view + Mileage helper).
- S-INVOICE-CUMULATIVE-TOTAL-LABEL + S-INVOICE-BACKDATE-WARNING SHIPPED (combined 2-commit arc: 8a761a8 C1 IN-FLIGHT registration + this C2 Bug 4 build + both sessions docs — see PROGRESS.md SESSION LOG rows for each) — **Bug 1 (cumulative total label):** verification-only; D163 incidental fix in S-MILEAGE-2B C6 already replaced label across all surfaces. Repo-wide grep `Cumulative Total|cumulative_total` across `app/ api/ lib/` returned zero hits; new "Lease-to-Date Distance" label confirmed at `app/admin/invoices/show.php:1576`. **Bug 4 (backdate warning):** investigation surfaced that PROGRESS.md:562 canonical rider ("`billing_period_start < lease.start_date`") differs from prompt's expected gap ("`billing_period_end < today`"); operator authorized option (C) **build both warnings**. Two reactive amber advisory banners landed in `app/admin/invoices/create.php` via new `_todayYmd()` helper + `backdateWarnings()` method + `<template x-for>` block: (a) `pre_lease_start` fires when period_start precedes lease.start_date (canonical D163 framing — pre-contract billing risk); (b) `past_period_end` fires when period_end < today (prompt framing — already-billed-period risk). Non-blocking, no server-side validation. D131 gate clean on every commit: doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 16/0/0 + PHP lint clean. Single .php file touched (UI-only). No schema / migration / `.env` / FF_ASSET_VERSION surface change.
- S-DOC-FRESHNESS-DISCIPLINE SHIPPED (3-commit arc: 342ec29 C1 IN-FLIGHT registration + 46ebb7e C2 smoke + REFERENCE.md §13.7 See-also gate extension + this C3 docs commit — see PROGRESS.md SESSION LOG row) — new `tests/_smoke_doc_freshness.php` (293 lines, 17/17 PASS exit 0) as the third D131 gate alongside parity + invariants + migrate-verify. Four-class staleness detection: Class 1 canonical file existence + readability + non-empty (7 checks); Class 2 SESSION LOG / CURRENT_SESSIONS cross-consistency (2 checks, all 17 SHIPPED labels currently present in PROGRESS.md, bundled-session-friendly); Class 3 tool-call markup leak scan (6 checks, one per canonical .md, code-fence-aware so legitimate `</invoke>` etc. inside ``` blocks are NOT flagged); Class 4 D136 IN-FLIGHT discipline (≤1 write-mode IN-FLIGHT, IN-FLIGHT-RO exempt). Injection test verified the smoke fires (synthetic `</invoke>` append to PREDEPLOY_CHECKLIST.md → exit 1; restore → exit 0). REFERENCE.md §13.7 See-also line 1237 extended to bridge the new smoke into D131; PROGRESS.md:294 D131 body preserved as-locked (S-BILLING-RATE-FIX provenance untouched). PROGRESS.md cleanup originally-scoped Part B was a NO-OP — P1 grep confirmed zero standalone artifacts since S-TEMPLATE-MILEAGE-DEFAULTS C3 side-fix already cleaned the known target at PROGRESS.md:167-170. D131 gate clean on every commit: doc_freshness 17/17 + PARITY OK + INVARIANTS OK I1-I10 + migrate 16 ok / 0 drift / 0 missing + PHP lint clean. No production .php / schema / migration / `.env` / FF_ASSET_VERSION changes.
- S-TEMPLATE-MILEAGE-DEFAULTS SHIPPED (3-commit arc: 32dc6f8 C1 IN-FLIGHT registration + b962f6a C2 migration + master mirror + this C3 docs commit — see PROGRESS.md SESSION LOG row) — pre-work scan confirmed zero NULL rows on `equipment_templates.default_mileage_rate` at session start (S-MILEAGE-RATE-ZERO-FIX 2026-05-06 had already backfilled production templates 1/3/4/5; the 7 zero-default seed templates IDs 6/7/10/11/12/14/17 all have zero active leases per P3 → intentional non-billable per D135 config 3). No backfill executed. Migration `202605122229_S-TEMPLATE-MILEAGE-DEFAULTS_not_null_default.sql` enforces `decimal(10,4) NOT NULL DEFAULT '0.0000'` (precision widening 8,4 → 10,4 aligns with `leases.mileage_rate_km` / `mileage_rate_miles` convention per S-LEASE-UNITS); includes `ff_assert_no_null_mileage_rates()` pre-flight SIGNAL guard for replay safety. Master file line 1694 mirrored in same commit per D87 + D127. **S-LOOKUP-RATES-PRODUCTION-INVARIANT (QUEUED optional) flipped SUPERSEDED** — NOT NULL DEFAULT 0 makes the invariant's "template default must be non-NULL" branch permanently trivially true; no remaining defensive surface to test. Side-fix: removed 4 stray tool-call closing-tag artifacts at PROGRESS.md:167-170 left by prior S-REEFER-RATE-AUDIT C2 edit (broke SESSION LOG table rendering). D131 gate clean on every commit: PARITY OK + INVARIANTS OK I1-I10 (10/10 PASS exit 0) + migrate 15 ok → 16 ok / 0 drift / 0 missing + post-apply `SHOW COLUMNS` decimal(10,4) Null=NO Default=0.0000 + zero NULLs post-apply. PHP lint N/A (no PHP files touched). No code, no test, no `.env` / FF_ASSET_VERSION surface change.
- S-REEFER-RATE-AUDIT SHIPPED (2-commit arc: f0a5599 C1 IN-FLIGHT upgrade RO→write + this C2 file + docs — see PROGRESS.md SESSION LOG row) — investigation (I1-I7) confirmed `scripts/seed_rate_cards.php` was NEVER run against prod DB (I7: only 4 customer-specific rate cards exist, no "Standard 2025" / "Reefer Specialist 2025" markers). Reefer rubric ($0.22/km seed) vs live ($0.18 template default + $0.15-$0.25 customer-negotiated range across 4 customers) is intentional divergence — seed = industry-standard reference; live = Mainland T&T operator pricing decisions (reefer at parity with dry van). Only ONE historical reefer mileage line ever billed (INV-94 at $0.18). **Disposition (C): comment-only alignment** — seed script $0.2200 value preserved + file-header @note block + 13-line inline comment above EQUIP_REEFER BASE_RATES row documenting divergence + S-REEFER-RATE-AUDIT 2026-05-13 reference. No DB writes; D14 active-lease-rate immutability preserved. D131 gate clean: php -l no errors + PARITY OK + INVARIANTS OK I1-I10 + migrate 15/0/0.
- S-LEASE-21-CLEANUP SHIPPED (2-commit arc: a8498ea C1 IN-FLIGHT upgrade RO→write + this C2 data + docs — see PROGRESS.md SESSION LOG row) — investigation confirmed lease 21 (CN-7B5Z5B-2026) is seed/test data (real activate→close delta = 1 min 13 sec, not 3 min; customer 3 Lepore Enterprise 555-phone + Phase 0.5 counter-drift culprit per KI #51; zero payments; mileage_rate_km=0, precharge_enabled=0). **Disposition (C): seed/test data, no real customer impact.** Data correction: INV-93 (id 162) draft → void + soft-delete (test artifact); lease 21 outstanding_balance $250.00 → $0.00 (residual drift); audit_log row id=1040 entity_type='lease_counter_correction'. Docs: KI #107 consolidating A2 + A4 with full disposition + references (NOT the #93 sketched in prompt — #93 was already taken by S-PROD-2 audit #18); RESOLVED markers appended to 4 embedded A2/A4 refs (3 in S-MILEAGE-RATE-VALIDATION SESSION LOG row + 1 in D133 trailing line). D131 gate clean: PARITY OK + INVARIANTS OK I1-I10 + migrate 15/0/0 + lease 21 OB=$0.00 + zero non-void invoices on lease 21. No code/schema/migration changes.
- S-MILEAGE-5 SHIPPED (3-commit arc: 7dec061 C1 IN-FLIGHT + e16d3f6 C2 tests + this C3 docs — see PROGRESS.md SESSION LOG row) — 20 hermetic Model B lifecycle scenarios at `tests/_smoke_model_b_lifecycle.php` (S01–S20 covering activation init + Invoice 1 emit + send stamp + drawdown at three boundary shapes + multi-invoice exhaustion + bypass + three close-refund cases per K-21 + mark-settled happy path + state-machine 409s + 422 validator via inline-predicate mirrors + $0.01 bcmath precision edge + Model B Lite + Disabled mileage D135 rows + K-18 Samsara fallback discipline + FIX_RESET fixture-mode warning capture + S20 end-to-end I10 cross-check). I10 invariant added to `tests/_smoke_billing_invariants.php` (count 9→10): credit branch `credit_notes.amount == leases.precharge_balance` + cash branch `audit_log new_values.amount == leases.precharge_balance`, half-cent tolerance. **K-21 future-I10 candidate CLOSED.** No production .php / schema / migration changes — pure test + docs against the surface frozen from S-MILEAGE-3-FIX-0. D131 gate clean: PARITY OK + INVARIANTS OK I1–I10 + samsara 16/16 + drawdown stress 6/6 + refund state machine stress 6/6 (no regressions) + migrate 15/0/0. No T1 walk this session (visual verification completed in S-MILEAGE-3-FIX-0 T1 9/9 PASS). Model B mileage refactor arc fully closed pending S-MILEAGE-3-ACCT-SPEC for CPA-blocked accounting spec text.
- S-MILEAGE-3-FIX-0 + T1 PASS — refund dispatch ordering fix + T1 visual walk 9/9 PASS (commits 3ad8c41 C1 IN-FLIGHT + this C2 fix + T1 + docs). C2 surgical reposition in api/v1/leases/close.php: moved the refund dispatch block from BEFORE the ADV-BILL-1 / legacy partial-end final-invoice generation to AFTER it. Refund block now re-SELECTs precharge_balance + precharge_refund_method via SELECT FOR UPDATE on the lease row (after InvoiceGenerator's drawdown emit has run during final-invoice generation), so refund fires only on the post-drawdown residual. Three cases now correctly distinguished: (a) precharge_enabled=0 → no refund (existing); (b) precharge_enabled=1 + residualBalance=0 → no refund needed (drawdown consumed full balance; method stays NULL — case observed in T1 Step 5 cash branch with FIX_STD on close-gen invoice); (c) precharge_enabled=1 + residualBalance > 0 → refund dispatch fires (case observed in T1 Step 8 credit branch with samsara_vehicle_id flipped NULL pre-close to skip close-gen drawdown — credit_note CN-CR-2026-00009 created for $77.78, settled_at stamped at close-commit per D-E ii). **T1 walk 9/9 PASS** with K-19 persistent user, fresh units 22 + 23 (units 14 + 21 deferred from halted walk), full lifecycle on both branches, Model C surface absent on both show pages (retirement-marker comments only). **KI #106 RESOLVED 2026-05-13** (refund dispatch ordering double-credit bug). **K-20 + K-21 locked** in KEY LEARNINGS. **D131 gate green throughout**: PARITY OK + INVARIANTS I1-I9 (9/9 PASS) + samsara distance 16/16 + migrate 15/0/0 + refund stress 6/6 PASS. S-MILEAGE-3 arc fully closed.
- S-MILEAGE-3-T1-HALT — refund dispatch ordering bug surfaced at T1 Step 5 (commit bda21ec) — KI #106 filed; lease 185 + invoices 240/241/242 cancelled/voided; fixture state restored; K-19 user preserved. Triggered S-MILEAGE-3-FIX-0 follow-up session (see above row).
- S-MILEAGE-3 SHIPPED (8-commit code arc + C9 docs: 6fcb859 C1 IN-FLIGHT + 1c57952 C2 ENUM ext + c045fc4 C3 close.php refund dispatch + mark_refund_settled.php + 2c727c4 C4 close UI picker + Mark Refund Settled button + dfb8b23 C5 priorExcessKm retirement + lease_close_adjustments DROP + 721fac1 C6 Smoke I9 + stress + ff07f31 C7 SPEC_FINAL.md close-refund rewrite + C8 SKIPPED + 4b4126f C9 docs — see PROGRESS.md SESSION LOG row) — Model B close-refund lifecycle complete + Model C plumbing fully retired. 17 DECISIONS rows locked (D168-D184: D-A through D-N + V1 preserve-not-zero + V2 status='completed' + V3 transaction atomicity). **D-B (i) manual deferred-settle** lock executed: cash branch stamps method='cash' with settled_at=NULL at close; mark_refund_settled.php endpoint stamps settled_at=NOW() when operator confirms physical disbursement. **D-C credit branch**: credit_notes row with source='precharge_refund' + AutoEntryBridge::onCreditNoteIssued + settled_at=NOW() at close-commit. **D-D state machine**: 409 PRECHARGE_REFUND_LOCKED + 422 PRECHARGE_REFUND_REQUIRED gates inside close transaction. **D-F + D-G retirement**: ~340 LOC removed from close.php + ~240 LOC from app/admin/leases/show.php + lease_close_adjustments table DROPPED (backup 0 rows per K-15) + master file CREATE TABLE swap. **Latent bug fixed**: api/v1/leases/show.php SUM(excess_distance_km) query (S-MILEAGE-2B C4 left this READ orphan post-column-DROP). **D-N I9 invariant**: closed lease + residual balance → non-NULL method + settled_at for credit branch; cash-branch NULL settled_at NOT a violation by design. 6/6 stress PASS. **C8 SKIPPED — D-I CPA-blocked**; S-MILEAGE-3-ACCT-SPEC follow-up session QUEUED for accounting JE spec text. FF_ASSET_VERSION 1.0.26 → 1.0.28 dev .env; PREDEPLOY_CHECKLIST.md A3 entry per K-14. D131 gate clean on every commit: PARITY OK + INVARIANTS OK (I1-I8 through C5, I1-I9 from C6) + migrate 13 → 15 ok / 0 drift / 0 missing. T1 visual walk pending operator per D-M pattern (8-step walk: K-19 persistent user; cash branch Steps 1-7 + credit branch Step 8 + Model C absence verification).

**2026-05-12:**
- S-MILEAGE-2B-T1 PARTIAL-PASS — 8-step T1 visual walk completed in preview by Claude Code (commits 85cde5f IN-FLIGHT + this C2 docs commit). 6/8 steps PASS + 1 PARTIAL (Step 7) + 1 PASS (Step 8). **Steps 1-4 PASS**: lease activated → precharge_balance init'd ($500) + Invoice 1 auto-generated with mileage_precharge $500 line + send stamped precharge_invoiced_at + balance unchanged + audit_log entries (entity_types: lease_precharge_balance_init #926, lease_precharge_invoiced_at_stamp #928). **Step 5 PASS**: PRECHARGE_LOCKED 409 fires on both precharge_amount AND precharge_enabled changes (D113). **Step 6 PASS**: Invoice 2 drawdown emit produced mileage_usage $222.22 + mileage_drawdown_credit $222.22 (POSITIVE amount + is_credit=1 per K-16 convention D166); lease.precharge_balance decremented $500.00 → $277.78; audit_log entity_type=lease_precharge_balance_drawdown #932 captures old/new + drawdown_amount. **Step 7 PARTIAL**: ✓ D-F Financial Summary breakdown renders correctly (Mileage usage / Precharge credit applied / Net mileage charge); ✗ D-L Drawdown Reconciliation panel has 2 display-only bugs surfaced + filed as KNOWN ISSUES #104 + #105 in PROGRESS.md (panel renders "Period Distance: 0.00 km" because invoice_line_items.mileage_distance is NULL — InvoiceGenerator db_insert tuple missing the mileage_* columns; AND panel renders the Model B Lite ELSE-branch note instead of pre/applied/post balance rows because the audit_log JSON_EXTRACT lookup fails under PDO param binding). Financial math is 100% correct on both invoices; only the display panel shows wrong values. **Step 8 PASS**: no "Mileage Review" text, no "Review Required" label, no `mileage_review_status` references in DOM; Send button is plain "Send Invoice" (D-I retirement clean). **Test user pattern (K-1)**: Manager-role test user (id=22, claude-t1-20260512-093519@fleetforge.test) created with disposable 16-char hex password, used for browser automation, soft-deleted at tear-down. Test lease 172 (T1-WALK-20260512-093721) created via SQL (operator's "via UI" preference noted but lease-create form is S-MILEAGE-1's surface, not S-MILEAGE-2B's — created via SQL for control), walked through full Model B lifecycle, cancelled at tear-down with lease_status_log entry. Test invoices 237 + 238 voided + soft-deleted with structured void_reason; Path B counters reversed. Fixture infrastructure (samsara.fixture_mode='1' + equipment_unit 14 samsara_vehicle_id='FIX_STD') flipped at setup, restored at tear-down (mode was already 1; vehicle ID back to NULL). Smoke gates green throughout: PARITY OK + INVARIANTS OK (I1-I8) + samsara distance 16/16 + migrate 13/0/0. **Carry-forward:** S-MILEAGE-2B-FIX-0 (queued for ~30 min display-only fix: 2-line InvoiceGenerator.php db_insert + 1-line show.php CAST AS UNSIGNED + optional I9 smoke invariant + visual T1 re-walk on patched panel; not gating S-MILEAGE-3).
- S-MILEAGE-2B SHIPPED (9.5-commit arc: 7acde72 C1 IN-FLIGHT + d4923c7 C2 ENUM/dispatch + a24cb49 C3 drawdown emit + Samsara + 64b37cb C3.5 silent-bug cleanup + 840c71f C4 Model C retirement + INV-87 void+regen + 6ed9529 C5 send gate retirement + f7b7e87 C6 Drawdown Reconciliation panel + Odometer card + Financial Summary breakdown + 6dd449c C7 I8 invariant + T8/T10 real coverage + 47acbb9 C8 FLEETFORGE_SPEC_FINAL.md rewrite + this C9 docs commit — see PROGRESS.md SESSION LOG row) — Model B drawdown lifecycle complete on Invoice 2..N + Model C plumbing wholesale retired. 21 DECISIONS rows locked (D147-D167: D-A through D-S + K-16 emit-shape clarification D166 + D-H refinement D167). K-18 KEY LEARNING locked (stress test pre-population hiding fallback-path bugs — surfaced via the C3.5 silent JOIN bug fix). T1 visual walk pending operator per D146 pattern (8-step walk). New S-PORTAL-MILEAGE-MODEL-B session QUEUED in Architectural follow-ups (final Mileage::monthlyAllowance deletion + portal Model B refactor). FF_ASSET_VERSION 1.0.26 → 1.0.27 dev .env; filed under PREDEPLOY_CHECKLIST.md A2 per K-14. D131 gate clean on every commit: PARITY OK + INVARIANTS OK (I1-I7 through C6, I1-I8 from C7) + samsara distance 16/16 + migrate 11 → 13 ok / 0 drift / 0 missing. INV-87 void+regen produced INV-2026-00094 at $1370.52 (was $1280.71 broken Model C draft — operator-confirmed amount delta per Model B Lite D135 every-km billing; no D14 conflict since INV-87 never sent).
- S-MILEAGE-2A SHIPPED (b602bd9 IN-FLIGHT registration + 253b294 C2 activate.php precharge_balance init + e1918df C3 InvoiceGenerator mileage_precharge emit + (b) cross-invoice uniqueness gate + engine markers + c8e459a C4 send.php precharge_invoiced_at stamp + PRECHARGE_ALREADY_BILLED 409 + 365d541 C5 T14/T15/T16 smoke ADD + b16c0fc C6 I7 D132-precharge-extension invariant + this docs commit) — 7-commit arc closing Invoice 1 Model B lifecycle end-to-end. C2 lands activation transaction extension (FOR UPDATE on lease per D20 + idempotent UPDATE under WHERE-clause guard + dedicated audit_log entity_type='lease_precharge_balance_init'); 6/6 stress PASS. C3 lands InvoiceGenerator emission with 3-clause gate (lifecycle + (b) cross-invoice uniqueness + billing_type exclusion) + 67-line engine marker block carrying forward-looking 2B drawdown contract (greppable via "S-MILEAGE-2B inserts drawdown logic"); 6/6 stress PASS. C4 lands send.php dispatch (NULL → stamp NOW(), NOT NULL → 409 PRECHARGE_ALREADY_BILLED with locked message wording) under FOR UPDATE on lease + dedicated audit_log entity_type='lease_precharge_invoiced_at_stamp'; PRECHARGE_LOCKED 409 D113 activates for free off the stamp; WHY-comment block at 409 site documents C3 (b) gate ↔ C4 409 paired defense-in-depth (defends against racy concurrent gen, manual UI/API line insertion, future regression weakening C3 gate); 5/5 stress PASS. C5 ADDs T14/T15/T16 to tests/_smoke_samsara_distance.php (preserving T8/T10/T12 placeholders as 2B carry-forward per REFERENCE.md §13.6 framing — operator overrode spec's REPLACE wording to ADD); smoke 13/13 → 16/16 PASS. C6 lands I7 invariant (D132 extension into precharge tier — precharge_enabled=1 must have precharge_amount > 0) + stress test using CREATE TEMPORARY TABLE clone trick (column types copied without CHECK constraints, allowing fault-injection of violation rows); INVARIANTS OK I1 → I7 (6/6 stress PASS). 10 D-decisions locked D137-D146 (D-A through D-J). Mid-session clarifications: D-D 409 status code (409 vs 422 for state-conflict family with D113/D19), D-B (b) uniqueness gate (advance-batch multi-draft gap), D-E ADD framing (preserve 2B placeholders), D-G I7 scope (D132 extension vs activation-init check — operator pick). FLEETFORGE_SPEC_FINAL.md Model A→Model B rewrite deferred to S-MILEAGE-2B per D144; no FF_ASSET_VERSION bump per D145. D131 gate clean on every commit: PARITY OK + INVARIANTS OK + migrate 11/0/0. **T1 visual sign-off (5-step walk per D146): ✓ PASS — operator-confirmed 2026-05-12.** Activate fresh lease → confirmed precharge_balance init in DB → Invoice 1 carries mileage_precharge line → send invoice + confirmed stamp + balance unchanged → re-edit lease → PRECHARGE_LOCKED 409 fires.
- S-MILEAGE-2A-SPEC-WRITE SHIPPED (86353d4 IN-FLIGHT + 17b2776 C2 spec + 3a9351c C3 Master Plan + K-16 + this docs commit) — docs-only 4-commit arc closing the K-16 mid-arc chat handoff requirement before operator closes a long claude.ai web chat that scoped the Model B refactor across multiple sessions. C1 IN-FLIGHT registered standalone per S-D136-COMMIT-DISCIPLINE (pushed to main immediately). C2 wrote full S-MILEAGE-2A spec into CURRENT_SESSIONS.md (replacing the prior short QUEUED entry of 4 lines with a full spec block: SCOPE / OUT OF SCOPE / HYBRID STATE / LINE ITEM SHAPE / DISPATCH CONTRACT NOTE / INTEGRATION TESTS / D132 EXTENSION / ARCHITECTURAL NOTES). C3 inserted `## Model B Mileage Refactor — Multi-Session Master Plan` section into PROGRESS.md between KNOWN ISSUES and NEXT SESSION STARTS WITH (forward-looking-plans area) + locked K-16 (Mid-arc chat handoff discipline) in KEY LEARNINGS table between K-15 and K-17 per K-17's K-number convention. C4 (this commit) ships out of queue. **Pre-work scan surfaced 4 factual discrepancies between operator's narrative and actual code state, operator authorized path B (correct + transparent SESSION LOG note):** D-1 dual S-MILEAGE-2A/2B extension point markers are in `app/admin/invoices/show.php` (PHP at 1769-1782, HTML at 2182), NOT in `lib/Billing/InvoiceGenerator.php` — verified via `git show 5dc2af2 --stat` (C3 of S-INVOICE-DISPLAY-COMPREHENSIVE touched only show.php); D-2 `mileage_precharge` ENUM already exists at FLEETFORGE_DATABASE_MASTER.sql:1898 (Model A vestigial retained for Model B per S-MILEAGE-MODEL-AUDIT 2026-05-04 — no migration needed); D-3 dispatch case already at show.php:1891 ('badge-info', placed by C3 of S-INVOICE-DISPLAY-COMPREHENSIVE as forward-looking display contract); D-4 supported/pending types lists corrected to match code (14 currently-supported values per show.php:1879-1881 + DB ENUM; pending: mileage_drawdown_credit + mileage_usage only — mileage_precharge is supported but not emitted). Net effect: S-MILEAGE-2A scope shrinks (schema + dispatch pre-wired) but session shape unchanged — engine emit + balance init + precharge_invoiced_at write + InvoiceGenerator markers + D132 invariant I7 + T8/T10/T12 tests. **D131 gate not runnable from this worktree** — no `.env` (gitignored in main repo); smoke tests fail with "Access denied" — worktree environment limitation surfaced to operator before C2; docs-only session has no code/schema/migration changes so D131 gate would not catch anything specific to this session. K-16 locked.
- S-FORK-CLOSE-RESOLVE SHIPPED (d0a0947 IN-FLIGHT registration + ab122eb merge commit + 834adc2 Phase 2.1 gitignore + c441aa6 Phase 2.2 archive + this docs commit) — 5-commit reconciliation arc. Phase 1 rebase aborted at step 2/11 due to journal-file conflicts (CURRENT_SESSIONS.md + PROGRESS.md SESSION LOG); operator switched to Option B merge commit. Merge `ab122eb` brings 11 worktree-branch commits (S-DOCS-CLUSTER + S-INVOICE-DISPLAY-COMPREHENSIVE + S-PREDEPLOY-CHECKLIST-CREATE arcs) into main alongside the 4 main-side commits (S-REVIEW-MILEAGE-TAX-FIX arc); 4 conflict hunks resolved per sign-off (IN-FLIGHT keep HEAD, Recent ship history descending UTC within date, SESSION LOG ascending UTC, KEY LEARNINGS K-14 before K-15 by K-number convention with chronology-inversion note inline). Phase 2.1 (834adc2): gitignored operator's `FleetForge Prompt Review.md` (file kept in working tree, stops tracking surface area). Phase 2.2 (c441aa6): created `scripts/archive/` with README + moved S-ACCT-FIX-A1 abandoned `fix_ar_drift_2026_05_07.php` into it. K-17 locked (branch divergence reconciliation discipline — merge commit, not rebase, when journal files are in scope). Pre-rebase backup tag `pre-rebase-S-FORK-CLOSE-RESOLVE-backup` preserved at origin for 30 days. D131 gate clean post-merge: PARITY OK + INVARIANTS OK (I1-I6 all PASS) + migrate 11/0/0. Worktree + local + remote `claude/kind-sanderson-4ed75d` branches deleted; main is the only branch.
- S-FORK-CLOSE SHIPPED (read-only diagnostic — no commits) — 7-step clean-state verification: branch state (surfaced divergence), worktree merges (HALT per operator directive), working tree (2 untracked files surfaced — Prompt Review + fix_ar_drift), CURRENT_SESSIONS.md state (zero IN-FLIGHT both branches; neither branch had all 4 day's sessions in Recent ship history — the documentation-layer visualization of the divergence), D131 gate on main (PASS — confirmed main internally clean despite doc divergence), GitHub sync (matched + 1 expected orphan branch), K-14 discipline check (PASS — A1 captured; S-REVIEW-MILEAGE-TAX-FIX had no missed pre-deploy obligations). Verdict: "FORK NOT CLOSED — 3 issues" → resolved in S-FORK-CLOSE-RESOLVE.
- S-PREDEPLOY-CHECKLIST-CREATE SHIPPED (commits 7d5e62d IN-FLIGHT registration + 2c5d146 file creation + C3 cross-refs commit) — created `FLEETFORGE_PREDEPLOY_CHECKLIST.md` as canonical pre-deploy operations file. 21 backfilled obligations across 9 categories (operator-framed planning count; 26 in final file after D + G expansion: A1 asset cache, B1-B5 prod .env keys, C1 DNS, D1-D9 AWS infra, E empty placeholder, F1 AR drift DEFERRED-TO-QBO, G1-G4 smoke, H1-H2 rollback, I1-I3 monitoring + J references). Each item carries Originating session + Surfaced into checklist sub-fields + "Original source:" pointer + Action + Owner + Status. K-14 locked (pre-deploy obligations category separation discipline, K-12 extension): bug-shaped → KNOWN ISSUES (in PROGRESS.md), session-shaped → CURRENT_SESSIONS, pre-deploy → PREDEPLOY_CHECKLIST. Cross-refs added to REFERENCE.md §0 LOCKED DECISIONS K-14 row + §1 SEVEN FILES "Deployment-time companion" note; CURRENT_SESSIONS.md Pre-flight check section extended with category-separation note + AWS Lightsail cutover entry extended with pointer. D131 gate: PARITY OK + INVARIANTS OK (I1-I6 all PASS).
- S-REVIEW-MILEAGE-TAX-FIX SHIPPED (3582e78 + 79934a0 + b1adbcd + docs commit) — 4-commit arc closing the dormant tax-divide-by-100 bug at api/v1/invoices/review_mileage.php:213-218 surfaced in S-MILEAGE-ALLOWANCE-ZERO-FIX C5. C1 standalone IN-FLIGHT registration (per S-D136-COMMIT-DISCIPLINE forecast — first session to commit IN-FLIGHT before any other edit). C2 fix + stress test (replaces bcdiv-by-100 with canonical bcround-bcmul pattern matching TaxCalculator.php:62; new tests/_stress_review_mileage_tax.php 14/14 PASS post-fix). C3 audited UPDATE for INV-2026-00092 line 175 (the one production row with bug applied — surfaced by K-15 data-path coverage scan; +$90.08 HST correction). C4 docs + K-15 lock. D131 clean throughout.

**2026-05-11:**
- S-INVOICE-DISPLAY-COMPREHENSIVE SHIPPED (commits 8bc4e5c + aabb087 + 5dc2af2 + df1bd20 + f9130a2) — comprehensive invoice show.php with $0-line draft hide (D-A), per-line + per-tax-row tax base reconciliation in "RATE% on $BASE" inline format with "Applied to N taxable lines totaling $X" tooltip (D-B), Credit Applications card between Line Items and Financial Summary rendering `credit_note_applications` JOIN (D-C, D-G), step-by-step balance due breakdown with dual S-MILEAGE-2A/2B extension markers (D-D), print-parity preserved (D-E), 3 reference invoices verified (INV-19/INV-13/INV-21) (D-F). Incidental fixes per D130: pre-existing tax-rate display bug (0.05% → 5% via ×100 conversion) corrected in C3; print overlay regression on browser Cmd+P fixed in C3-T1-FIX by extending app.css @media print hide list with `.search-modal`+overlay siblings (universal — applies to every admin print). K-13 locked. FF_ASSET_VERSION bumped 1.0.25 → 1.0.26 (uncommitted, .env gitignored).
- S-DOCS-CLUSTER-2026-05-11 SHIPPED — bundled docs commit closing three small queued sessions in one shape: S-D136-COMMIT-DISCIPLINE + S-D135-REFERENCE-PROMOTE + S-D130-EXTENSION. Each was single-commit / ≤20 min / touched REFERENCE.md; bundling avoided ceremony tax. Also exercised S-D136-COMMIT-DISCIPLINE's own discipline — IN-FLIGHT registration committed to main standalone (bd8dbb6) BEFORE first content edit. D-A: D136 wording extension in REFERENCE.md §0 (Registration commit requirement clause). D-B: D135 promotion (§0 index row + §13.8 subsection heading labeled with D135 + See-also extension). D-C: D130 contraction extension (PROGRESS.md row extended + new REFERENCE.md §0 row carrying combined expansion+contraction wording). D-D: D136 backfill into PROGRESS.md DECISIONS table. D131 gate: PARITY OK + INVARIANTS OK (I1-I6 all PASS).

**2026-05-07:**
- S-DOC-STATUS-RECONCILE-CLOSE SHIPPED (15865c4, fast-forward merged from session/S-DOC-STATUS-RECONCILE-CLOSE-20260507-0740 branch per D136 isolation fallback) — closes 4 documentation gaps surfaced by S-QUEUE-STATUS-RECONCILE diagnostic (3 SESSION LOG backfills + 3 CURRENT_SESSIONS status flips). Locks K-12 (documentation divergence as bug class). Extends §Active session format with `(DB writes)` annotation convention per S-ACCT-FIX-A1's organic precedent. **Wording correction (S-DOC-STATUS-RECONCILE-CLOSE-FIXUP)**: original entry said "S-ACCT-FIX-A1 zombie IN-FLIGHT entry on main" — empirically incorrect, the IN-FLIGHT registration was carried over from the parallel agent's working tree but never committed to main. Carryover block was removed in the FIXUP commit. See S-DOC-STATUS-RECONCILE-CLOSE-FIXUP SESSION LOG row + S-ACCT-FIX-A1 abandonment row.
- S-DOC-STATUS-RECONCILE-CLOSE-FIXUP SHIPPED (this commit) — corrective fixup post-merge of S-DOC-STATUS-RECONCILE-CLOSE. Removed S-ACCT-FIX-A1 carryover IN-FLIGHT block from CURRENT_SESSIONS.md, corrected misleading "zombie on main" wording in 2 places (this Recent ship history entry + S-DOC-STATUS-RECONCILE-CLOSE SESSION LOG row in PROGRESS.md), added S-ACCT-FIX-A1 abandonment SESSION LOG row, queued S-D136-COMMIT-DISCIPLINE for the working-tree-vs-committed-state discipline gap surfaced by this incident.
- S-QUEUE-STATUS-RECONCILE SHIPPED (no commit — read-only diagnostic, IN-FLIGHT-RO under D136) — first post-D136 IN-FLIGHT-RO session. Cross-referenced 32 session labels; surfaced 4 doc-divergence gaps (closed in S-DOC-STATUS-RECONCILE-CLOSE) + bonus finding: parallel agent S-ACCT-FIX-A1 IN-FLIGHT registration mid-diagnostic verified D136 collision-check works correctly + organic Touching format extension `(DB writes)` annotation.
- S-MULTI-AGENT-DISCIPLINE-IMPL SHIPPED (f195bee) — locks D136 (multi-agent discipline: hybrid single-agent serialization + branch isolation fallback) + K-11 (lock-discipline-before-frequency learning). Upgrades CURRENT_SESSIONS.md status schema with IN-FLIGHT / IN-FLIGHT-RO entries, multi-line entry format, and Pre-flight check section. Backfills S-ACCT-AUDIT SESSION LOG row to close the dangling reference in D136. D135 §0 promotion deferred to separate session (S-D135-REFERENCE-PROMOTE queued). Discipline takes effect immediately for next session.
- S-LEASE-GPS-COST SHIPPED (71c3e5c + 21c7c58 + 05266e7 + C4) — adds per-lease GPS tracking add-on (gps_opt_in tinyint default 1 + gps_cost decimal default 1.00). Per-day billing rhythm: amount = gps_cost × billing_days. Engine emits 'gps' line item (ENUM extended) when opt_in=1 AND cost>0. Existing leases backfilled to opt_in=1 / cost=$1.00 per Option (i) — auto-bill on next cycle. Stress test 4/4 PASS. D-A through D-G locked.
- S-MILEAGE-ALLOWANCE-ZERO-FIX SHIPPED (2168bd5 + 764abf1 + ef050e7 + C4) — engine-side fix for the silent-skip class on Model B Lite leases (rate>0 + allowance=0). Closed KNOWN ISSUE #103. D135 locked. Multi-agent reconciliation: parallel agent's commit ef050e7 combined C2+C3; my C1+C2 stand alone. Side-finding queued as S-REVIEW-MILEAGE-TAX-FIX (dormant tax bug in review_mileage.php).
- S-INVOICE-CREATION-UX SHIPPED (044ffef + 6feb94c + cdb59ca + 430fd91) — 3 issues from real-use testing: C1 docs-only VALIDATION GAP classification (KNOWN ISSUE #103, since RESOLVED above); C2 period auto-fill on invoice create form; C3 Generate Invoice button on lease profile.
- S-CURRENT-SESSIONS-FILE SHIPPED (4e7da02) — created FLEETFORGE_CURRENT_SESSIONS.md as active session queue companion to PROGRESS.md.
- S-LOOKUP-RATES-NAMESPACE-COMPLETE SHIPPED (d83c4e3) — D134 architectural lock + KNOWN ISSUE #102 close + K-1 KEY LEARNINGS extraction.
- S-MILEAGE-RATE-VALIDATION SHIPPED (714e5d6 + 9291d6b + 11476c4 + 61f23df) — defensive engine + API validation + smoke invariants for the mileage rate tier.
- S-LOOKUP-RATES-NAMESPACE SHIPPED (7a45534 + 7941148 + 32293dd) — rate-lookup endpoint namespace fix.

**2026-05-06:**
- S-MILEAGE-RATE-ZERO-FIX SHIPPED (bc4db87, 23268f7) — C2 (engine + smoke) and INV-27/INV-84 voids contracted out, deferred to S-MILEAGE-RATE-VALIDATION

**2026-05-02:** *(retroactive backfill added 2026-05-13 via S-PROD-2-DOCS-RECONCILE — the original ship was tracked only in PROGRESS.md SESSION LOG; surfacing here so AWS Lightsail cutover chat readers can locate it in the rolling window)*
- S-PROD-2 SHIPPED — production-readiness instrumentation (full detail in PROGRESS.md SESSION LOG row at line 104). Sentry SDK at lib/Observability/Sentry.php with init() / captureException() / captureMessage() + before_send PII scrub for bcrypt $2y$ hashes, ENC: AES ciphertext, 12 sensitive key names; wired into api/bootstrap.php + includes/header.php + app/portal/includes/header.php + 19 cron files. SES bounce/complaint webhook at api/v1/webhooks/ses_notifications.php (Aws\Sns\MessageValidator signature check, SubscriptionConfirmation auto-confirm, permanent+complaint → email_disabled=1 on customers + portal_users, transient → email_bounces audit row only). Mailer::isEmailDisabled() pre-send guard. Manager re-enable via api/v1/customers/reenable_email.php. Key rotation runbook at docs/runbooks/key_rotation.md (5 secrets: FF_MFA_SECRET_KEY, AWS access keys, DB password, SES SMTP credentials, Cloudflare API token). Logrotate runbook at docs/runbooks/logrotate_setup.md. Staging deferred per D78. Schema in db_migrations/S-PROD-2_ses_bounce_handler.sql (email_bounces table + email_disabled columns; counted in current migrate 17). DECISIONS D75-D78 locked. Unblocks PREDEPLOY_CHECKLIST.md B3/B4/B5/D7/I1 (reference accuracy flipped 2026-05-13 via S-PROD-2-DOCS-RECONCILE after S-CHECKLIST-DRIFT-FIX C2 introduced a phantom QUEUED entry based on stale info).

(Older entries archived in FLEETFORGE_PROGRESS.md SESSION LOG.)

---
End of CURRENT_SESSIONS file.
