-- ============================================================================
-- S-QBO-23 — Tax Remittance JE Sync (FF → QBO) (Phase QBO-11 / 2 of 2)
-- ============================================================================
--
-- CLOSES Phase QBO-11. Scope: tax_remittance JEs route through the existing
-- JournalEntryPusher (S-QBO-21) — no new Pusher class. Sibling of S-QBO-22
-- (Fixed Asset JE sync); same no-new-Pusher pattern.
--
-- Spec §8.15: "When FF posts a tax remittance JE (via TaxFilingService in
-- S035), the JE pushes via §8.10. Accountant files the actual GST34 return
-- through QBO's NETFILE workflow — FF doesn't file directly, just supplies
-- the data and remittance JE."
--
-- K-22 verified at pre-flight: TaxFilingService::recordRemittance (line
-- 448-454) ALREADY emits source_type='tax_remittance' + source_id=
-- acc_tax_remittances.id via JournalEntryService::create(post_immediately=
-- true). That create() call auto-enqueues to QBO via the S-QBO-21 hook.
-- 'tax_remittance' is ALREADY in the acc_journal_entries.source_type ENUM
-- (added pre-QBO-arc) AND is NOT in JournalEntryPusher /
-- JournalEntryEnqueuer BRIDGE_DERIVED_SOURCE_TYPES — so it flows through to
-- push with no code-path gating needed. NO ENUM ALTER required this session
-- (unlike S-QBO-22 which had to add 'impairment').
--
-- Migration motion (single):
--   1. Seed marker setting quickbooks.sync_mode.tax_remittance='inherit_je'
--      for symmetry with S-QBO-22's quickbooks.sync_mode.fixed_asset marker.
--      Documents that tax_remittance JEs inherit sync gating from
--      quickbooks.sync_mode.journal_entry — there is NO independent tax
--      remittance sync pipeline. Operator-visible in
--      /admin/quickbooks/settings Card 4 (Per-Entity Sync Modes), which
--      already renders the tax_remittance row (added in S-QBO-22 audit fix).
--
-- Per D-QBO-23-1: JournalEntryPusher::buildQboPayload PrivateNote builder
--   generalized from the S-QBO-22 FA-only inline block into a dispatcher
--   buildSourceNoteSection(sourceType, srcId) routing to FA helper OR new
--   buildTaxRemittanceNoteSection. Single try/catch in step 6. Tax-remittance
--   section format:
--     "TAX-REMIT remit#5 type=gst_hst period=2026-01-01..2026-03-31 amount=$12500.00 method=online_banking"
--
-- Per D-QBO-23-3: Admin UI generalized from S-QBO-22's single "Show FA only"
--   filter chip to a source-type chip group (All / Fixed Asset / Tax
--   Remittance) on /admin/quickbooks/journal_entries. API source_filter
--   accepts fa|tax_remittance. FA KPI strip generalized to a "By source
--   type" strip showing FA + tax-remittance counts.
--
-- Per D-QBO-23-4 (defense-in-depth re-confirmation, sibling of D-QBO-22-4):
--   JournalEntryPusher::TAX_REMITTANCE_SOURCE_TYPES ∩ BRIDGE_DERIVED_SOURCE_TYPES
--   = ∅ (confirmed via grep at pre-flight). Smoke asserts the regression
--   guard explicitly at both Pusher + Enqueuer constants.
--
-- @session  S-QBO-23
-- @spec     FLEETFORGE_QUICKBOOKS_SPEC.md §8.15 (Tax Remittance) + §8.10 (JE)
-- @decision D-QBO-23-1 (PrivateNote enrichment dispatcher generalizing the
--                       S-QBO-22 FA-only block; routes FA + tax-remittance),
--           D-QBO-23-2 (NO ENUM ALTER — tax_remittance already present;
--                       NO new Pusher — reuses JournalEntryPusher per §8.15),
--           D-QBO-23-3 (admin UI = source-type chip group All/FA/Tax
--                       Remittance + generalized "By source type" KPI strip),
--           D-QBO-23-4 (tax_remittance NEVER bridge-derived — regression
--                       guard via smoke at both Pusher + Enqueuer constants)

-- ─────────────────────────────────────────────────────────────────────
-- B1: Seed marker setting quickbooks.sync_mode.tax_remittance
-- ─────────────────────────────────────────────────────────────────────
-- Mirror of S-QBO-22's quickbooks.sync_mode.fixed_asset='inherit_je'.
-- Value 'inherit_je' is a MARKER (not consumed by Enqueuer/Pusher);
-- documents that tax_remittance JEs inherit sync mode from
-- quickbooks.sync_mode.journal_entry. Operator-visible in
-- /admin/quickbooks/settings Card 4 so the tax-remittance JE flow is
-- discoverable without reading spec §8.15. Per D-QBO-23-1 / D-QBO-23-3.

INSERT INTO `settings` (`key`, `value`, `value_type`, `group_name`, `is_public`, `is_sensitive`, `description`)
VALUES
    ('quickbooks.sync_mode.tax_remittance', 'inherit_je', 'string', 'quickbooks', 0, 0,
     'S-QBO-23 / D-QBO-23-1 marker: tax-remittance JEs (from TaxFilingService::recordRemittance) inherit sync mode from quickbooks.sync_mode.journal_entry. No independent tax-remittance sync pipeline per spec §8.15. Operator-visible documentation only.')
ON DUPLICATE KEY UPDATE `description` = VALUES(`description`);
